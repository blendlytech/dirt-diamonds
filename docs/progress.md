# Progress & Next Steps

Session-orientation file for the AI models. **Keep it lean:** at session end, append a COMPACT entry to the Session Log (what shipped, commit, gates, next step + model — a few lines, not a blow-by-blow). Full detail belongs in [progress_archive.md](progress_archive.md). When the Session Log grows past ~30 entries, move the oldest to the archive.

*(Archived: Phases 0–12, the HS Grade System, the full High School arc HS-0…HS-6, Act 1 onboarding, the phone Events/Messages split, the HS calendar slice, and UI slices A/B — all in [progress_archive.md](progress_archive.md).)*

---

## Current Status (2026-07-11)

**All prior arcs COMPLETE and committed.** Current focus: the `docs/game-idas/GAME_IMPROVEMENTS.md` UI/content roadmap (Slices A+B shipped; **Onboarding-2 tutorial-overlay slice COMPLETE + SIGNED OFF**; **content arc DIRT-1 "The On-Ramp" COMPLETE — Sonnet build REVIEWED + SIGNED OFF with one review fix (Fable, 2026-07-11); user playtests remain**; D–F, DIRT-2+, hustle minigames remain — see Roadmap below).

- **Gate suite (all green as of the DIRT-1 review):** MonteCarlo **345/345** (MLB bit-identity guard PA 48384 / H 10969 / ER 5237, byte-exact, inert — no `Assets/Simulation` touch; ⚠ one run of eight tallied 344/345 with no `[FAIL]` tag captured — a rare stochastic-check flake, see the parked seam in Follow-ups), GrittyEvents **286/286** (90 events — DIRT-1's 11 added), CoreLoop **22/22**, NeedsDecay **102/102**, SchemaValidator **111/111** — schema **v13**.
- **Live save:** day 3, clock paused (the user has started playing — the day-2 note is superseded) — untouched by this review's live-verification boot.
- ⚠ **Session hygiene:** an auto-committer is active AND parallel sessions occur — run `git status` + `git log` FIRST every session; never trust a stale "uncommitted" label.

### Standing workflow rules

- **Model roles:** **Opus** = design docs/specs. **Sonnet 5** = content batches, data/constants edits, UI builds. **Fable 5** = engine + anything touching the calibrated core, and the standing review of every Sonnet build.
- Any `Assets/Simulation/` touch ⇒ mandatory `run_monte_carlo_batch` re-run; the MLB bit-identity guard must stay byte-exact unless a re-band is explicitly sanctioned.
- Content batches gate through `check_event_graph_integrity`; schema changes through `validate_sqlite_schema`; playtest pacing/copy complaints = data edits (Sonnet) + harness re-run.

---

## ROADMAP — remaining work (dependency-ordered, model-tagged)

Source: `docs/game-idas/GAME_IMPROVEMENTS.md` (user-refreshed 2026-07-11). Already resolved from that doc: text messages = DONE; item-popup removal = MOOT (no popup infra exists); autosave-per-event = DONE by architecture (surfaced in Slice B's note).

| # | Slice | Model flow | Notes / dependencies |
| --- | ----- | ---------- | -------------------- |
| 1 | **Slice D — UI audio foundation** | Opus spec → Fable build | Greenfield (zero audio infra): autoload `UiSfx`, pooled `AudioStreamPlayer`s, placeholder tones, button/tab wiring. Unlocks Slice B's parked volume slider. |
| 2 | **Onboarding-2 — tutorial overlay + self-explaining needs UI** (supersedes the retired `tut_*` phone-thread tutorial) | Opus T-0 spec → Fable T-1 + T-3 DONE → Sonnet T-2 BUILT → **Fable review SIGNED OFF (2026-07-11, two review fixes: click-through + clock soft-stop)** — awaiting user playtest (A2b) | `docs/design/onboarding_tutorial_overlay.md`. Slice complete: overlay scene + Main bridge + `tutorial_step` KV + Settings→Replay, all gates re-run green. |
| 3 | **Slice G — real-time clock + time pause/speed** | Opus design → **Fable BUILT G-1+G-2+G-3 (2026-07-11, see Session Log)** — awaiting user playtest | Metronome model shipped as designed (one disclosed §4.1 deviation: hustle sessions soft-pause the clock instead of hard-gating advance — advance IS their forfeit). G-4 optional "pause each morning" toggle remains. |
| 4 | **Slice E — phone tier visual theming** | Opus spec → Fable build | Per-tier bezel/screen themes off the Phone_State tier BurnerPhone already caches. |
| 5 | **Slice F — UI animation/immersion pass** | Opus motion spec → Fable build | Tab/message tweens, funds ticker, "different UI per location". Depends on A (done) + D. |
| 6 | **Content arc — "The Dirt" (criminal underworld), lead batch DIRT-1** | Opus spec DONE → Sonnet authored → **Fable review SIGNED OFF (2026-07-11, one review fix: `dirt_sticky_fingers` day-2 gate)** — DIRT-1 COMPLETE; next batch = DIRT-2 "The Fix" spec (Opus) | Gambling-debt on-ramp CONVERGES into the shipped `compromised_syndicate` spine (harness-proven at +365 on both on-ramps); `in_the_life` master flag; no `detection_risk` touch. Beats autopilot-resolve until the avatar event-choice UI ships (§9 deferred — a strong next-slot candidate). |
| 7 | **Hustle minigames** — Texas Hold'em, narcotics stages, fencing, robberies | Opus spec per minigame → build → Fable review | `Tools/HoldemHarness` already exists as the Hold'em math core; `gritty_events.md` Hustle State Machine rules apply. After D–F unless re-prioritized. |

**Follow-ups riding playtest verdicts (not slices):**

- **Development practice-lever retune** — the calendar slice's top seam: max ~78 practice h/season vs the 500h e-fold conversion. **Sonnet 5** data edit + mandatory MC re-run, after the user's calendar-pacing verdict.
- **At-Bat Read-Input model** — code-complete + signed off; open: §7.4 manual playtest (user) → then the re-band (**Fable 5**).
- **College/pro tier calendars** (if wanted after HS pacing verdict) — Sonnet build → Fable review, same `TryGetLeagueScheduleDay` seam.
- Parked small seams for any Sonnet slot: college batch reads `hs_scouted`; pro-arc rekindle content; 3 deferred 12c cleanups; MC-harness FAILED-recap-on-tail — **now upgraded from cosmetic to wanted:** during the 2026-07-11 T-2 review one MC run in eight tallied **344/345** (exit 1) while the other seven runs and every guard stayed byte-exact green; the failing check's name wasn't captured, and a tail recap would have named it. One MC check is rarely stochastic — worth identifying and seeding.

## VERY NEXT STEPS

1. **User plays** (the remaining user-driven exits — the T-2 review is DONE):
   - Checklist **Session A2 + rewritten A2b** (`docs/hs_manual_playtest_checklist.md`): onboarding beats back on days 2–14, NO Getting-Started cards ever, the new Free-hours line states + self-explaining Needs card + the T-2 overlay itself (fresh save day-1 walkthrough, quit/resume mid-tutorial, Settings → Replay).
   - Consolidated UI + Slice B: top bar day+funds, phone Calendar tab + status-bar clock, school mandated-note row, Settings → Save Now shows "Saved ✓ — day N". *(The `847d676` Player/League phone tabs are user-confirmed — layout renders correctly.)*
   - Calendar pacing: browse March 1 (season start) + a summer week; confirm ScheduleScreen's locked rows match CalendarScreen.
   - Longer-standing: checklist Sessions B–F; At-Bat §7.4 playtest.
2. **Next build slot (user picks):** Slice D audio (Opus spec → Fable build), DIRT-2 "The Fix" spec (Opus), or the avatar event-choice UI (Opus spec first — makes the new criminal beats player-chosen instead of autopilot-resolved; the arc doc names them its strongest first customer).

---

## Session Log (compact — full detail in the archive)

- **2026-07-11 — DIRT-1 review SIGNED OFF with one review fix (Fable 5):** the entry below's "UNCOMMITTED" label is stale — the auto-committer swept the whole batch into `a4a01c3` (recycled message). Verified against `criminal_underworld_content_arc.md` §4–§7: all 11 events avatar-scope, §5.1 flag lifecycle exact, `detection_risk` untouched, every `person_stat` name in the parser's closed switch, `hs_started` writer real, `the_bookie` row + registry header done, and the three World proofs sound (the disclosed `marker_mirror` gate is a legit test-fixture-only fix). **Review fix — day-2 slot steal (doc §5.3 violated):** `dirt_sticky_fingers`'s bare `funds<200` gate holds on day 2 for the Destitute backstory ($50 floor, 12% of rolls), and `dirt_…json` sorts alphabetically BEFORE `hs_onboarding_…json` while the dispatcher fires the first satisfied definition in file order — a weight-0.08 roll at stealing the avatar's day-2 slot from the weight-1.0 `hs_first_day` (≈1% of fresh saves, and it would flake checklist A2's pinned day-2 beat). Fixed with an `hs_started`+14d anchor (`dirt_lookout`'s own belt-and-braces idiom, disclosed in the batch header) + a new harness check running the real `ConditionEvaluator` over every DIRT-1 gate against a worst-case Destitute day-2 subject — pins the class, not just the instance. **Disclosed, not fixed:** `dirt_clean_break` ships on `coach_reyes` (doc-sanctioned) yet is fully reachable at tier 0, where every previously shipped coach_reyes event is age/flag-gated out — an HS save can receive the pro coach's thread; DIRT-2-era polish candidate (per-tier split or `family_mom`). Gates: build 0/0, GrittyEvents **286/286** (90 events), `git diff --stat -- Assets/Simulation` empty, live boot green ("gritty events 90 (dispatcher polling)", schema v13/WAL/FK, errors empty, clock restored Paused, save un-advanced — it reads day 3: the user has played since the day-2 note). **Next: user plays (dirt beats autopilot-resolve into the phone over longer sessions); next build slot = Slice D audio (Opus spec → Fable), DIRT-2 spec (Opus), or the avatar event-choice UI spec (Opus) — user picks.**
- **2026-07-11 — DIRT-1 "The On-Ramp" AUTHORED (Sonnet 5, UNCOMMITTED), per Opus 4.8's `docs/design/criminal_underworld_content_arc.md` §5/§6/§7.3:** New `Assets/Narrative/Events/Content/dirt_underworld_events.json` — 11 `scope:avatar` `hustle`/`family` events implementing the gambling-debt spiral exactly to the §5.1 flag-lifecycle table (`owes_the_book`→`dodging_debt`/`running_errands`→**`in_the_life`**, converging into the shipped `compromised_syndicate`→`syndicate_shakedown` spine via `dirt_debt_sold`), plus petty-crime texture (`dirt_lookout`, `dirt_sticky_fingers`→`dirt_busted` arrest absence), fallout (`dirt_family_finds_out`, `dirt_hot_merch`, `dirt_street_rep`), and the `dirt_clean_break` exit. `detection_risk` never touched anywhere in the batch (§2 collision #1 honored). Added test-only `cooldown_days`/`min_days_since` beyond the doc's explicit numbers on 6 slots whose every choice left their own gate re-satisfiable (`dirt_the_marker`, `dirt_lookout`, `dirt_busted`, `dirt_family_finds_out`, `dirt_hot_merch`, `dirt_street_rep`, `dirt_clean_break`) — disclosed in the batch's own header comment, no `check_event_graph_integrity` #4 violations. `the_bookie` ("Sal", role `friend`) added to `contacts.json` + its `//` header, zero `ContactRegistry.cs` touch. `GrittyEventsHarness` gained `RunDirtUnderworldArcChecks` (§7.3): static pins against the real parsed file (flag lifecycle choice-by-choice, avatar scope, no orphaned flags, no `detection_risk`), plus three dynamic `World` proofs using deterministic weight-1.0 mirrors (the established `RunCascadeAndWriterChecks` technique, needed since the real weights are <1 and the +365 timing claims need exact-day arithmetic) — **both on-ramps converge on `syndicate_shakedown` at exactly +365** (bribe path day 2→367; debt path `chase_the_pot`→`duck_the_call`→`dirt_debt_sold` day 2→12→42→407), and **the bust benches** (`dirt_busted`'s arrest absence computes `until_day` correctly). Whole-folder count pins bumped 79→90 in both `RunContentChecks` and `RunContactRegistryChecks`. **One self-caught bug during the harness build:** the debt-path mirror's `marker_mirror` had no gate against re-firing after its first "duck," so it refired every day for 395 days, continuously resetting `dodging_debt`'s clock and starving `dirt_debt_sold`'s `min_days_since:30` window — fixed by adding a test-only `flag_inactive:dodging_debt` gate (a single-choice deterministic mirror always re-picks the same branch, unlike the real content's weighted 3-choice autopilot draw, which converges away from repeat-ducking almost every real playthrough — so this is a test-fixture-only fix, not a real-content bug). Gates: `dotnet build` on the full solution **0 warnings/0 errors**, `GrittyEventsHarness` **285/285**, `git diff --stat -- Assets/Simulation` empty (calibration-inert by construction, no MC re-run mandated). **Not yet done:** the live godot-MCP boot check (§7 gate 5) — left for Fable's review pass. **Next: Fable 5 reviews against the design doc + re-runs §7's gate set + live boot, sign-off.**
- **2026-07-11 — Content arc STARTED: "The Dirt" criminal-underworld spec (Opus 4.8):** user picked the criminal arc to lead the content push (over College-bridge / pro-career-depth). Wrote `docs/design/criminal_underworld_content_arc.md`: a multi-batch spine (DIRT-1 on-ramp → DIRT-2 game-fixing @tier≥2 → DIRT-3 fencing/turf → DIRT-4 bust & going straight), a reusable **authoring contract** (§4), and **DIRT-1 "The On-Ramp" in full buildable detail** (§5) — ~11 `scope:avatar` `hustle` events, a gambling-debt spiral whose ducked-debt branch **converges into the shipped `compromised_syndicate` spine** (so `syndicate_shakedown@+365` becomes reachable by a 2nd on-ramp), `in_the_life` master flag, a `dirt_busted` arrest-absence for real bench-time teeth, and a `dirt_clean_break` exit. Two shipped collisions caught + routed: (1) `caught_juicing` gates on `detection_risk>70` with **no PED-use check**, so criminal content uses **boolean jeopardy flags, never `detection_risk`** (re-gate on `ped_active` deferred to DIRT-4); (2) syndicate stays anonymous on `unknown`, one named associate `the_bookie` ships on existing role `friend` (pure content, zero `ContactRegistry.cs` touch). Grounding: read all 9 content batches + the full parser/evaluator/applier + tier-ladder + integrity SKILL — confirmed the closed vocabulary (Sonnet can't add consequence types), the person_stats-are-write-only rule, the NULL-team tier-leak (avatar scope is immune), and the `interest`-no-op-on-avatar constraint, all folded into §4. Zero code/schema touch. **Next: Sonnet 5 authors `dirt_underworld_events.json` + `the_bookie` row + §7.3 harness checks/count pin → Fable 5 reviews + gates (§7).**
- **2026-07-11 — Onboarding-2 T-2 review SIGNED OFF with two review fixes (Fable 5):** Verified against the overlay doc §3/§5/§7: resume math correct (KV 1-based, `step - 1` at the one `Main.TryOpenTutorial` boundary — Sonnet's self-caught off-by-one fix confirmed; absent-key→0→clamp lands step 1), §3.3 save-compat guard exact (`absent && current_day > 2` ⇒ -1), all six of Main's resolver paths re-verified against the live .tscn trees (TimeControlBar, PhoneTabs, NeedsCard, FundsCard, SaveCard, shell), step copy verbatim mod stripped markdown, signals connected/disconnected per conventions, phone tab-index logic reorder-safe (`GetIndex()`-derived). **Review fix 1 — the overlay was an input wall:** root `mouse_filter` was Stop, but steps 3–6 instruct "Open Bank"/"Open Calendar → Plan Today" and doc §3.2/risk-1/risk-3 all require tapping tabs and scrolling under the scrim (the doc's own §3.1 sketch said Stop — the sketch contradicted the doc's behavior text; behavior wins). Now Ignore: full click-through, the card's own buttons unaffected. **Review fix 2 — the clock ran under the tutorial:** nothing held `TimeControlBar`'s `timeFlows` gate, so a fresh save (boots 08:00 @ Normal = 5 min/sec) would burn ~10 game-hours of day-1 needs decay during a ~2-min read — and with fix-1's wall up, Pause was unreachable; doc §7 risk-1 explicitly assumes "a paused, modal surface". Added `GameManager.TutorialOverlayOpen` (set in `Open`/`Close`), a **soft stop** in the Slice-G idiom — new `timeFlows` term + "Paused — tutorial open" bar face, player Speed undisturbed, Skip stays enabled, deliberately NOT in `CanAdvanceDay` (harness-safe plain bool, mirrors the hustle deviation). Checklist A2b un-skipped + 2 new lines covering both fixes. **Independent gate re-run: GrittyEvents 267/267, CoreLoop 22/22, NeedsDecay 102/102, Schema 111/111, MC 345/345 with the MLB guard byte-exact (PA 48384 / H 10969 / ER 5237) — `git diff --stat -- Assets/Simulation` empty. ⚠ One MC run in eight tallied 344/345 (exit 1, no `[FAIL]` line captured); 7 consecutive full-capture re-runs all 345/345 ⇒ pre-existing rare flake in a stochastic check, unrelated to this slice (sim untouched, guard byte-exact) — parked seam upgraded (FAILED-recap-on-tail would have named it).** Build 0/0. **Next: user plays A2 + A2b (incl. the two new fix lines); next build slot = Slice D audio (Opus spec → Fable) or content arc (Opus → Sonnet → Fable) — user picks.**
- **2026-07-11 — Onboarding-2: `TutorialOverlay` BUILT (T-2, Sonnet 5, per Opus 4.8's `docs/design/onboarding_tutorial_overlay.md` §3/§5):** New `Assets/UI/TutorialOverlay.tscn`+`.cs` — a permanent, self-hiding sibling in `Main.tscn` (the `EquipmentShopScreen` scrim+centered-Card idiom), holding the 8-step script verbatim from the doc as data (`TutorialStep[]`, markdown emphasis stripped since `BodyLabel` is a plain `Label`, not BBCode). Spotlighting never reaches into another scene's tree: the overlay emits `TargetRectRequested(int target)` every `_Process` tick while a targeted step is open (doc §7 risk 1 sanction — "a handful of `GetGlobalRect()` calls on a paused modal surface", not the sim loop); `Main` resolves the target against the `BaseballDashboard`/`BurnerPhone` subtrees it already owns (same shared-ancestor bridge as `HustleLaunchRequested`/`ShopOpenRequested`) and answers with `GetGlobalRect()` or an empty `Rect2` (not currently visible — e.g. wrong phone tab) which degrades to a centered, unhighlighted card. Scrim-cutout implemented as four dynamically-resized `ColorRect` notches around the target rect rather than a `_Draw` punch (doc explicitly allows either). New `GameStateKeys.TutorialStep` KV: 1-based step number persisted on every advance (Close writes -1); `Main.TryOpenTutorial` applies the doc §3.3 save-compat guard (absent key + `current_day > 2` ⇒ write -1, never show) and converts the persisted 1-based value to the overlay's 0-based `Open(fromStep)` index at that one boundary. Settings tab gains a third card, **Replay tutorial**, wired via a new `BurnerPhone.TutorialReplayRequested` signal (same bridge pattern) → `TutorialOverlay.Open(0)`. **Self-caught bug during review: an initial off-by-one** (passing the persisted 1-based value straight into the 0-based `Open()`) would have resumed one step ahead of where the player quit — fixed before reporting done (`step - 1` at the Main.cs boundary). **Disclosed scope call:** the doc's optional per-step `DiagramContainer` (§3.1, used by steps 3–5) is populated with a static 5-label decay-order legend rather than a richer diagram — the doc itself says the real teaching is the live spotlighted Needs card, and the three steps' bodies already state the ordering. Zero `Assets/Simulation/` touch, zero schema change (KV row only, matches Slice-G's `TimeOfDayMinutes`/`TimeSpeed` template exactly). `dotnet build`: **0 warnings, 0 errors**. **Gate harnesses NOT re-run this pass** — that and the manual playtest (fresh save walkthrough, quit-mid-tutorial resume, pre-existing mid-career save must NOT show it, Replay button) are next, owned by Fable's review per the doc's T-3 stage description.
- **2026-07-11 — Onboarding-2: needs UI + Free-hours line BUILT (T-1) + `tut_*` tutorial RETIRED (T-3) (both Fable 5, per Opus 4.8's `docs/design/onboarding_tutorial_overlay.md`; retirement of `a8be02b` content user-approved same day):** **T-1** — `ScheduleScreen`'s Free-hours line now three states (0 free = danger *"No free hours — nobody eats today"*, 1..4 = warning *"tight"*, ≥5 = plain explainer; **band harness-sourced, NOT the doc's first-pass 1..3** — `simulate_utility_decay`'s scripted-week fixture bottoms Hunger at 0 even with 5 free hours, so warn through 4, exported as `LowFreeHoursMax`); Needs card gains *"Listed fastest-draining first."* caption, a 20%-line `ColorRect` marker per bar (scene-authored at 0.2, re-anchored in `_Ready` from live `NeedsEngine.CriticalThreshold`), per-row drain/fix tooltips in the .tscn (Hunger's "~16h to critical" is the harness-measured hour-16, per the doc's no-guessed-numbers rule), critical rows turn danger-red + a caption names them (mask-dirty-flagged, plural-aware), card-level acceleration/stress caption (2.5× = `MaxStressModifier`); new shared `UiColors` (danger matches Main.cs's banner red; Slice E absorbs it later). Node paths mapper-verified. **T-3** — `getting_started_events.json` deleted (83→79 events), `RunTutorialArcChecks` + its count pins removed from the gritty harness, `hs_first_day` back on **day 2** (emergent — the tut events just ate the slot; zero event edits), checklist header/A2 day numbers reverted + **Session A2b rewritten** for the overlay slice (T-2 lines marked skip-until-built), `getting_started` contact **kept** as a dormant hint thread (contacts.json + SKILL.md wording updated to say so). **One disclosed deviation from the doc's §6: SKILL.md check #8 KEPT** (the doc's parenthetical said remove it) — it's a general contact-resolution authoring rule whose harness check still runs (now pinning 79); deleting the documentation of a real leak class because its trigger batch retired would be self-harm. Gates: build 0/0, GrittyEvents **267/267**, MC **345/345** MLB guard byte-exact (PA 48384 / H 10969 / ER 5237), CoreLoop 22/22, Schema 111/111, NeedsDecay 102/102 (sourcing run; `Assets/Simulation` untouched — `git diff --stat` confirms UI/harness/content only). **Next: Sonnet 5 builds T-2 → Fable reviews; user plays A2 + A2b.**
- **2026-07-11 — Day-1 phone tutorial Stage-3 review SIGNED OFF (Fable 5):** the Stage-2 build landed as `a8be02b` (auto-committer; the entry below says UNCOMMITTED — stale). Every design-doc §10 item verified: §7.2 alternation (`tut_welcome@2`→`hs_first_day@3`→…→`tut_wrap@8`) + curated `hs_first_day@2` + veteran/College suppression all green in an **independent gate re-run** (GrittyEvents **277/277**, MC **345/345** with the MLB bit-identity guard byte-exact PA 48384 / H 10969 / ER 5237); content-vs-contract clean (3 choices ea., §6.2 flag closure, §6.3 self-contained, §5 copy verbatim modulo the two disclosed reconciliations, both re-verified against the engine: `ContactRegistry.cs` role switch IS closed, choice `text_message` IS singular); `check_event_graph_integrity` #1–#7 pass (avatar scope + `==` gate ⇒ no NULL-team leak; every event self-blocking ⇒ no unpaced loop). **One review fix:** §6.2 told Sonnet to add `getting_started` to "the skill's contact list" — the skill had no such list; added a new check #8 (contact-resolution + shipped contact ids) to `check_event_graph_integrity/SKILL.md`. The `ImagePath`→`LogoPath` harness hunk confirmed a legit compile fix against the real `SchoolCatalog.cs` field. **Next: user plays checklist A2+A2b; then Slice D audio (Opus spec → Fable) or content arc (Opus → Sonnet → Fable) — user picks.**
- **2026-07-11 — Day-1 phone tutorial BUILT (Sonnet 5, UNCOMMITTED), Stage 2 of `docs/design/day1_phone_tutorial.md` (Opus 4.8):** `getting_started_events.json` — 4 avatar-scope events (`tut_welcome`→`tut_daily_loop`→`tut_game_day`→`tut_wrap`, weight 1.0, all `tier==0`), flags-only consequences, `getting_started` contact added to `contacts.json`. Zero engine touch as designed, with two reconciliations against the actual shipped code (both noted in the batch's own header comment): (1) the design doc proposed `role:"system"` for the contact — `ContactJson`'s role parser is a closed switch that throws on an unrecognized string, so per the doc's own contingency this ships on the existing `role:"unknown"` instead; (2) the doc's near-final copy listed TWO delayed texts on one choice each of 3 events, but a choice's `text_message` is a singular field in the engine (one string or one `{body,delay_days}`, never an array) — merged each pair into one message at `delay_days:1`, preserving all the content. Harness: two whole-folder count literals 79→83, new `RunTutorialArcChecks` (content pins + a combined World run proving `tut_welcome@2`→`hs_first_day@3`→alternation through day 8 exactly per the design doc's §7.2 table, deterministic since every beat in both chains is weight 1.0; the skip path forced via `ResolveChoice`; veteran suppression — corrected mid-build: pre-seeding only `tut_done` does NOT suppress `tut_welcome` since each link gates on its own predecessor flag, not the terminal one, so the real invariant is all four flags set; tier suppression at College). GrittyEvents **267→277/277**. Also fixed a pre-existing unrelated build break inherited from `522829f` (school-catalog harness referenced `SchoolDefinition.ImagePath`, which doesn't exist — corrected to the real `LogoPath` field) so `dotnet build` could run at all. Gates: build 0/0, GrittyEvents 277/277, MC 345/345 byte-exact (`git diff --stat -- Assets/Simulation` empty ⇒ inert by construction), CoreLoop 22/22, NeedsDecay 102/102, SchemaValidator 111/111 (no schema touch). Live godot-MCP boot on the real save: **"gritty events 83 (dispatcher polling)"** logged, schema v13/WAL/FK intact, save at day 2 clock-paused, errors empty, clean stop — save left untouched (no day-advance) so the tutorial/onboarding beats stay fresh for the user's own playtest. `docs/hs_manual_playtest_checklist.md` updated: header counts (253→277 GrittyEvents total, 79→83 events, NeedsDecay 94→102), Session A2's day numbers shifted +1 (`hs_first_day`@3, report card ~day 28-29, rival seed ~day 21-22) to reflect `tut_welcome` now winning day 2, new **Session A2b** covering the tutorial thread/skip/heir-reexposure. **Next: Fable 5 Stage-3 review (design doc §10) — ordering/alternation/tier-gate/closure re-check + independent gate re-run + sign-off; then user plays checklist A2+A2b.**
- **2026-07-11 — Slice G BUILT: G-1+G-2+G-3 (Fable 5, UNCOMMITTED):** the full real-time clock per `docs/design/real_time_clock_slice_g.md`. **G-1:** pure `GameClock` in Core (Godot-free, compiled by 3 harnesses; speed ladder Slow/Normal/Fast = 1/5/30 min per real-sec, Faster=120 enum-reserved unsurfaced; one-roll-per-frame back-pressure), `time_of_day_minutes`/`time_speed` Game_State KV keys + boot seed (absent → 08:00 @ Normal), promoted **`GameManager.CanAdvanceDay`** + `InteractiveGameInFlight` (dashboard reports its game task into the shared gate). **Disclosed deviation from the design's §4.1 sketch: `HasPendingHustleSession` is NOT in the hard gate — the hustle screens' shipped contract is that advancing the day IS the no-deal forfeit (NarcoticsHustleScreen abandoned-run note; the can't-afford start panel has no other exit, so a hard gate would soft-lock). The clock treats it as an auto-pause SOFT stop instead; Skip stays enabled.** **G-2:** `TimeControlBar` scene/script replaces the dashboard's Play/Skip pair — Pause/Resume, speed toggles, Skip Day (autopilot ON, verbatim semantics), contextual Play Game (midnight rolls advance with autopilot OFF so game days park pending → auto-pause + button), "TUE APR 14 · 2:47 PM" readout + reason-annotated pause face, settle-wait + `DaySettled`/`PlayGameRequested` signals (at-bat machinery stays in the dashboard); checkpoint persistence via `GameManager.PersistTimeOfDay()` on SaveNow/_ExitTree/pause/speed-change, with a rare-cadence observability print. **G-3:** phone status bar renders the same clock (dirty-flagged day+minute). Gates: build 0/0 (sln ×8), `git diff --stat -- Assets/Simulation` EMPTY → MC inert by construction, CoreLoop 22/22, GrittyEvents 256/256, NeedsDecay 102/102, Schema 111/111 (KV keys ≠ DDL). **Live-verified on the real save: an unattended midnight roll advanced day 1→2 (sqlite-monitored) and the day-2 onboarding event fired off the clock-driven tick (auto-pause face up); pause/speed checkpoint writes print-verified with correct pairs (user clicked the bar mid-run); boot restore resumes the exact persisted minute+speed (booted 628 @ Slow, flowed at exactly 1 min/s); errors empty. Save then RESTORED from backup to pristine day-1 (keys stripped) so the onboarding beat stays unconsumed.** Known seam: the godot MCP's stop is a hard kill, so the `_ExitTree` persist specifically wasn't witnessed — same 4-line path as the click-verified writes; a normal window close exercises it. **Next: user plays (clock/pause/speed/Skip/contextual Play, phone clock lockstep, quit+reload resume); G-4 optional "pause each morning" (Sonnet or Fable, small); next build slot = Slice D audio (Opus spec first) or tutorial thread (Opus outline first) — user picks.**
- **2026-07-11 — Slice G design doc (Opus) — DONE + UNBLOCKED:** `docs/design/real_time_clock_slice_g.md`. Core decision: the intraday clock is a **presentation/driver-layer metronome** — a pure `GameClock` (Core, Godot-free) accumulates real-time→game-minutes and, on midnight, calls the **unchanged** `Clock.AdvanceDay()`; the sim gains NO hour/minute resolution (rejected re-plumbing thousands of entities). Play/Skip reconcile losslessly: Skip Day retained as instant step; continuous clock = paced/interruptible Skip; "Play Game" becomes the contextual at-bat launch. Safety-critical seam = promote the dashboard's private day-advance-blocked predicate to a shared `GameManager.CanAdvanceDay` (auto-pause on any decision gate); one-roll-per-frame back-pressure. Reframe surfaced: 1min=1sec ⇒ 24 real-min/day, so the clock is ambient/tactical, NOT generational traversal (that stays discrete Skip + offseason fast-sim). **User chose Option B (persist time-of-day) — and it's cheaper than first framed: `Game_State` is KV, so `time_of_day_minutes`+`time_speed` are new KV KEYS (like `avatar_practice_credit`), NOT a DDL change — no `user_version` bump, no SchemaValidator change, no migration (absent-key → 08:00 default); checkpoint-only writes via existing `SaveNow`/`_ExitTree`.** Sub-slices G-1 (pure model + KV seed/restore + shared gate) → G-2 (TimeControlBar replaces Play/Skip) → G-3 (phone time-of-day) → G-4 optional "pause each morning". No `Simulation/` touch ⇒ MC inert. **Next: Fable builds G-1.**
- **2026-07-11 — `847d676` verification (Fable 5):** the owed gate record for the parallel-session PlayerScreen/LeagueScreen extraction. `git status` clean at `b68454f` (the doc/skill commit atop it), diff scope confirmed pure `Assets/UI/` + `Main.cs` wiring (no sim/schema touch ⇒ no MC re-run mandated). **Build 0/0** (game + all 6 harness projects), **live boot (godot MCP) on the real save clean:** schema v13, WAL, FK on, day 1 fresh avatar, 48 teams, 819 life-sim NPCs, 79 gritty events dispatcher polling, ~18 s run, **errors empty**. Known-benign: Steam DLL-absent notice (dev environment, pre-existing). **User eyeball same day: Player/League tab layout confirmed correct.** `847d676` is fully closed; UI work may layer on it.
- **2026-07-11 — PlayerScreen/LeagueScreen extraction (`847d676`, parallel session):** dashboard scouting card + stat panels moved into self-contained phone tabs; shipped without a gate run — gate-verified AND user-confirmed live same day (entry above). **CLOSED.**
- **2026-07-11 — Slice B Save+Options (`a7bd114`, Fable 5):** `GameManager.SaveNow()` checkpoint intent + phone Settings tab (Save Now, "Saved ✓ — day N", parked volume slider). All gates green; NeedsDecay 102/102.
- **2026-07-11 — Travel-time checkpoint (`02177f1`, another session's build):** per-trip `TravelTime` + `DaySchedule.TravelHours` + planner forcing; gate-verified then committed pre-B (NeedsDecay 94→102).
- **2026-07-11 — Slice A UI consolidation (`67c9f7b`, Fable 5):** TopBar day+funds, CalendarScreen → phone tab, phone status-bar clock, School slider → mandated note folded into the hours budget.
- **2026-07-11 — HS calendar slice (`b1bc77d`, Fable 5):** sparse 26-game Tue/Fri spring season, Mon–Fri school with breaks, calendar-forced planner blocks, browsable CalendarScreen; MC 345/345, HS band held, MLB guard byte-exact.
- **2026-07-10 — Phone Events/Messages split + per-choice outcomes/delayed texts + category headings + History tab (Sonnet 5 builds, Fable 5 reviews SIGNED OFF):** GrittyEvents 232→256; §4.3 never-gates invariant held; feed capped at 200 cards.
- **2026-07-10 — Act 1 onboarding arc (Opus design → Sonnet build → Fable review) + school-life batch:** `SubjectField.Tier`/`.Gpa`, 11-event onboarding chain, rookie batch `tier>=2` re-gate, +13 school-life events (79 total). **Critical dispatcher fix: avatar hoisted to sweep front — it had been starved of ALL gritty events for 112 days by scope:any NPC saturation of `MaxFiresPerDay`.**
- **2026-07-08…10 — High School arc HS-0…HS-6 + every disclosed seam closed (schema v11→v13):** person layer + backstory/family + phone/marketplace/items economy + GPA drift + dating/pregnancy/child-rearing + NPC autonomy + PersonEffects sim layer + PersonLedger. MC guard byte-exact throughout; the planned HS-6 re-band moved NO band. Exit: user plays `docs/hs_manual_playtest_checklist.md`.
- **2026-07-08 — HS Grade System + freshman start age 16 (`a3fc6cb`-era):** grade derived from age, merit-based graduation, held-back mechanic.
- **Pre-2026-07-08 — Phases 0–12 (see archive):** engine foundation, all harnesses, At-Bat Read-Input model (code-complete, playtest pending), Phase-12 "Saleable" 12a–12d.
