# Progress & Next Steps

Session-orientation file for the AI models. **Keep it lean:** at session end, append a COMPACT entry to the Session Log (what shipped, commit, gates, next step + model — a few lines, not a blow-by-blow). Full detail belongs in [progress_archive.md](progress_archive.md). When the Session Log grows past ~30 entries, move the oldest to the archive.

*(Archived: Phases 0–12, the HS Grade System, the full High School arc HS-0…HS-6, Act 1 onboarding, the phone Events/Messages split, the HS calendar slice, and UI slices A/B — all in [progress_archive.md](progress_archive.md).)*

---

## Current Status (2026-07-11)

**All prior arcs COMPLETE and committed.** Current focus: the `docs/game-idas/GAME_IMPROVEMENTS.md` UI/content roadmap (Slices A+B shipped; **Onboarding-2 tutorial-overlay slice COMPLETE + SIGNED OFF**; **content arc DIRT-1 "The On-Ramp" COMPLETE — Sonnet build REVIEWED + SIGNED OFF with one review fix (Fable, 2026-07-11); user playtests remain**; D–F, DIRT-2+, hustle minigames remain — see Roadmap below).

- **Gate suite (all green as of the DIRT-1 review):** MonteCarlo **345/345** (MLB bit-identity guard PA 48384 / H 10969 / ER 5237, byte-exact, inert — no `Assets/Simulation` touch; ⚠ one run of eight tallied 344/345 with no `[FAIL]` tag captured — a rare stochastic-check flake, see the parked seam in Follow-ups), GrittyEvents **286/286** (90 events — DIRT-1's 11 added), CoreLoop **22/22**, NeedsDecay **102/102**, SchemaValidator **111/111** — schema **v13**.
- **Live save:** day 3, clock paused (the user has started playing — the day-2 note is superseded) — untouched by this review's live-verification boot.
- ⚠ **Session hygiene:** an auto-committer is active AND parallel sessions occur — run `git status` + `git log` FIRST every session; never trust a stale "uncommitted" label.

### Standing workflow rules

- **Model roles:** **Opus** = design docs/specs. **Sonnet 5** = content batches, data/constants edits, UI builds. **Fable 5** = engine + anything touching the calibrated core, and the standing review of every Sonnet build.
- Any `Assets/Simulation/` touch ⇒ mandatory `run_monte_carlo_batch` re-run; the MLB bit-identity guard must stay byte-exact unless a re-band is explicitly sanctioned.
- Content batches gate through `check_event_graph_integrity`; schema changes through `validate_sqlite_schema`; playtest pacing/copy complaints = data edits (Sonnet) + harness re-run.

---

## ROADMAP — remaining work (dependency-ordered, model-tagged)

Source: `docs/game-idas/GAME_IMPROVEMENTS.md` (user-refreshed 2026-07-11). Already resolved from that doc: text messages = DONE; item-popup removal = MOOT (no popup infra exists); autosave-per-event = DONE by architecture (surfaced in Slice B's note).

| # | Slice | Model flow | Notes / dependencies |
| --- | ----- | ---------- | -------------------- |
| 1 | **Slice D — UI audio foundation** | Opus spec DONE (`docs/design/ui_audio_slice_d.md`) → **Fable D-1 BUILT + swept into `c0b1d62`** → **D-2/D-3 DEPRIORITIZED to back of queue (user directive 2026-07-11)** | Greenfield: autoload `UiSfx` (2nd autoload, `Assets/UI/`, ProcessMode.Always), `Master`→`SFX` bus, 8-voice pooled round-robin, **procedurally-synthesized placeholder tones (zero committed assets)**, `UiSound` enum→stream vocab (swap-for-real free). D-1 un-parked Slice B's `VolumeSlider`. **§3.5 resolved to the recommended default: `user://settings.cfg` ConfigFile (per-device) — user may still veto to Game_State KV (~5-line flip).** Remaining: D-2 (`SceneTree.NodeAdded` auto-wire Tap/TabSwitch + `"silent_ui"` opt-out) → D-3 (curated semantic stings) → D-4 optional music bus (deferred). |
| 2 | **Onboarding-2 — tutorial overlay + self-explaining needs UI** (supersedes the retired `tut_*` phone-thread tutorial) | Opus T-0 spec → Fable T-1 + T-3 DONE → Sonnet T-2 BUILT → **Fable review SIGNED OFF (2026-07-11, two review fixes: click-through + clock soft-stop)** — awaiting user playtest (A2b) | `docs/design/onboarding_tutorial_overlay.md`. Slice complete: overlay scene + Main bridge + `tutorial_step` KV + Settings→Replay, all gates re-run green. |
| 3 | **Slice G — real-time clock + time pause/speed** | Opus design → **Fable BUILT G-1+G-2+G-3 (2026-07-11, see Session Log)** — awaiting user playtest | Metronome model shipped as designed (one disclosed §4.1 deviation: hustle sessions soft-pause the clock instead of hard-gating advance — advance IS their forfeit). G-4 optional "pause each morning" toggle remains. |
| 4 | **Slice E — phone tier visual theming** | Opus spec → Fable build | Per-tier bezel/screen themes off the Phone_State tier BurnerPhone already caches. |
| 5 | **Slice F — UI animation/immersion pass** | Opus motion spec → Fable build | Tab/message tweens, funds ticker, "different UI per location". Depends on A (done) + D. |
| 6 | **Content arc — "The Dirt" (criminal underworld)** | DIRT-1 COMPLETE (Fable signed off 2026-07-11) → **DIRT-2 "The Fix" SIGNED OFF (2026-07-13, zero fixes)** → **DIRT-3 "Product & Turf" SIGNED OFF (2026-07-15, zero fixes)** → **DIRT-4 "The Bust & The Door Out" COMPLETE (2026-07-15): Sonnet content half + Fable engine half BUILT, Fable review SIGNED OFF, zero fixes (see Session Log). THE ARC IS CLOSED.** Remaining = user playtests (the beats need tier ≥ 2 / an arrest to fire). | Gambling-debt on-ramp CONVERGES into the shipped `compromised_syndicate` spine (harness-proven at +365 on both on-ramps); `in_the_life` master flag; no `detection_risk` touch. **DIRT-3 makes hustling the arc's THIRD on-ramp: 9 reactive events read all seven `HustleService` engine flags and give the durable ones narrative resolutions (robbery arrest teeth, turf injury teeth, the turf-shaped door out).** Beats autopilot-resolve until the avatar event-choice UI ships (§9 deferred — a strong next-slot candidate). |
| 7 | **Hustle minigames DEPTH pass + robberies** | Opus spec DONE (`docs/design/hustle_minigames_depth_pass.md`, §5.1/§3.1 LOCKED, §11 seams resolved) → **R-1 BUILT (Fable 5) + N-1 BUILT (Sonnet 5) 2026-07-12** → **R-2/R-3 BUILT (Sonnet 5) 2026-07-13, swept into `3f3bdae` → Fable 5 review SIGNED OFF (2026-07-13, zero fixes)** → **F-1 BUILT + SIGNED OFF 2026-07-13** → **N-2 supplier haggle BUILT (Fable 5, 2026-07-13 — see Session Log; EV-neutrality LOCK harness-proven, mean cost 10.007)** → **F-2 fencing acquisition BUILT (Sonnet 5, 2026-07-13)** → **R-4 hot_goods write side SHIPPED** (confirmed via the baseline-87 harness check, 2026-07-15) → **JewelryExchange 4th robbery target SIGNED OFF (Fable 5, 2026-07-15, `d8c073b` — see Session Log)**; remaining = user playtests + Hold'em polish (deferred) | **RECONCILED 2026-07-12: Hold'em, Narcotics, Fencing all ALREADY SHIP wired** (sim cores + screens + `WorkActivity` selector — the roadmap's "unbuilt" premise was wrong, like the event-choice UI). Depth pass = deepen the 3 shipped games + add net-new **Robberies** (the one true gap). **R-1 robbery pure resolver + `HustleHarness` band DONE.** **N-1 narcotics re-up loop DONE.** **R-2/R-3 robbery screen + press-your-luck beat DONE** (`RobberyScreen.tscn/.cs`, `HustleService.BuildRobberyContext`/`ApplyRobberyResolution`, `WorkActivity.Robbery`, ScheduleScreen dropdown + Bank-tab quick-launch button) — makes R-1 fully playable. Hold'em = presentation-only, deferred. |

**Follow-ups riding playtest verdicts (not slices):**

- **Development practice-lever retune** — the calendar slice's top seam: max ~78 practice h/season vs the 500h e-fold conversion. **Sonnet 5** data edit + mandatory MC re-run, after the user's calendar-pacing verdict.
- **At-Bat Read-Input model** — code-complete + signed off; open: §7.4 manual playtest (user) → then the re-band (**Fable 5**).
- **College/pro tier calendars** (if wanted after HS pacing verdict) — Sonnet build → Fable review, same `TryGetLeagueScheduleDay` seam.
- Parked small seams for any Sonnet slot: college batch reads `hs_scouted`; pro-arc rekindle content; 3 deferred 12c cleanups; MC-harness FAILED-recap-on-tail — **now upgraded from cosmetic to wanted:** during the 2026-07-11 T-2 review one MC run in eight tallied **344/345** (exit 1) while the other seven runs and every guard stayed byte-exact green; the failing check's name wasn't captured, and a tail recap would have named it. One MC check is rarely stochastic — worth identifying and seeding.

## VERY NEXT STEPS

1. **User plays** (the remaining user-driven exits — the T-2 review is DONE):
   - Checklist **Session A2 + rewritten A2b** (`docs/hs_manual_playtest_checklist.md`): onboarding beats back on days 2–14, NO Getting-Started cards ever, the new Free-hours line states + self-explaining Needs card + the T-2 overlay itself (fresh save day-1 walkthrough, quit/resume mid-tutorial, Settings → Replay).
   - Consolidated UI + Slice B: top bar day+funds, phone Calendar tab + status-bar clock, school mandated-note row, Settings → Save Now shows "Saved ✓ — day N". *(The `847d676` Player/League phone tabs are user-confirmed — layout renders correctly.)*
   - Calendar pacing: browse March 1 (season start) + a summer week; confirm ScheduleScreen's locked rows match CalendarScreen.
   - Longer-standing: checklist Sessions B–F; At-Bat §7.4 playtest.
2. **Slice D §3.5 — built on the recommended default** (`user://settings.cfg` ConfigFile, per-device). Veto to Game_State KV remains a ~5-line flip in `UiSfx.LoadVolume`/`SaveVolume` if wanted.
3. **Playtest D-1 — PARTIALLY CONFIRMED (user, 2026-07-11): the release preview blip is audible** (synth + bus + slider wiring proven by ear). Still open: level tracks the slider position, 0 mutes, **quit + relaunch restores the saved volume**.
4. **CORRECTION (Opus, 2026-07-11): the avatar event-choice UI ALREADY SHIPS** — the "next slice" premise was wrong (mechanism was already live; see Session Log). User picked the follow-on **prominence/immersion pass** → Opus spec `docs/design/event_choice_prominence_pass.md`, **§5 locked (badge + banner, no auto-switch)**, **P-1/P-2/P-3 BUILT same session (Sonnet 5)** → **Fable 5 review SIGNED OFF (2026-07-11, one fix: `PortraitView` pre-tree NRE on the pending card; Sonnet's phone-internal banner placement APPROVED — see Session Log)**. Gates re-run green post-fix (build 0/0, GrittyEvents 286/286, MC 345/345, sim+narrative diffs empty, clean live boot on the real day-3 save). **ONLY remaining exit: user's eye-gate** (badge/banner/red face/portrait legibility, and picking a chip clears every cue).
5. **User priority directive (2026-07-11): step away from audio/polish "little things."** D-2 auto-wire + D-3 stings, Slice E theming, and Slice F animations all move to the BACK of the queue (D-1 stands as shipped, swept into `c0b1d62`). After the event-choice UI, the queue is the big gameplay items: hustle minigames (full Texas Hold'em, narcotics stages, fencing, robberies) and DIRT-2+ content.
6. **R-2/R-3 robbery screen — Fable 5 review SIGNED OFF (2026-07-13, zero fixes; see Session Log).** The Layer-2 touch was verified as exactly §5.3's enumerated surface; crew-rep resolution and `robbery_bust` wiring both mirror the Narcotics precedent correctly. Remaining exit: user playtest (all three approaches, bust vs clean-run messages, press-your-luck beat).

---

## Session Log (compact — full detail in the archive)

- **2026-07-15 — DIRT-4 "The Bust & The Door Out" BUILT (Sonnet 5 content + Fable 5 engine) + Fable 5 review SIGNED OFF, zero fixes — THE CRIMINAL ARC IS CLOSED:** **Content half (Sonnet):** 6 events appended to `dirt_underworld_events.json` exactly per spec §5 — the OR-split indictment pair (`dirt_the_indictment_a` reads `cooperated_with_league` for the lighter deal: 2d/4d arrests vs `_b`'s 5d/7d — the informant's discount holds at every rung, DIRT-2's forward hook fulfilled), `dirt_the_record_follows` (pure texture, no mechanical teeth of its own per the two-systems-one-flag rule), the door-out split into `dirt_going_straight` (clears `in_the_life`, −$5000) + `dirt_going_straight_buyout` (gates `compromised_syndicate` since EventChoice has no per-choice prereq — spec-sanctioned; its buyout is the arc's ONE content clear of the syndicate marker, −$12000), and the heir-pivot `dirt_the_next_generation` (author's option taken, gates `record_arrest` only). §5.4 consolidation: `set_flag record_arrest` added to both branches of `dirt_busted` + `dirt_robbery_fallout` (`narcotics_arrest` already had it — harness-verified) ⇒ every arrest in the game now marks the one canonical record. Neither `record_arrest` nor `banned_for_life` is cleared anywhere in the whole Content folder (harness-scanned). PED re-theme NOT taken (disclosed; the four `detection_risk` events are byte-untouched). **Engine half (Fable — the arc's FIRST `Assets/Simulation` touch):** `PromotionManager` avatar-gate block reads the two flags once per offseason pass via the indexed `BaseballQueries.LoadActiveFlagPlayerIds`, only when the avatar is otherwise eligible: `banned_for_life` ⇒ promotion-ineligible (the same cohort-skip as the pending-succession case — keeps team+tier, never relegated, the v1 stance); else `record_arrest` ⇒ the avatar's `A` is docked `PromotionProfile.RecordArrestPenalty` (**3.0** first-pass — under SwapMargin 5.0, so it kills borderline climbs, never a real talent gap) before ranking. Avatar-only; NPC sweep untouched; new consts `RecordArrestFlagName`/`BannedForLifeFlagName` on `PromotionManager`. **Gates (both halves): build 0/0; GrittyEventsHarness 345→363/363** (count literals 110→116 at both pins, 18 new checks: OR-split eligibility both directions via real ConditionEvaluator, discount inequality, consolidation sweep, terminal-flags folder scan, sole-syndicate-clear, fresh-save structural inertness at day 2 AND 9999 on an empty flag set, cooldowns 6/6, `until_day` mirrors for the 2d and 7d extremes); **MC batch 345→352/352** (new `RunPromotionRecordSuite`, 7 checks in two spread-0 no-op worlds where the avatar is the only possible mover: ban blocks a MAX avatar + no relegation, lifting ONLY the ban while `record_arrest` stays proves cause + ban-dominates-dock + dock-is-not-a-wall in one stroke; a marginal 56/56/56 avatar sits at the closed-form A=106 vs bar 105 — a runtime fixture guard re-margins loudly on any retune — docked to 103 ⇒ not promoted, cleared ⇒ promotes, exactly the one matched swap moves); **MLB bit-identity guard byte-exact (PA 48384/48384 H 10969/10969 ER 5237/5237)** + both 9c/9d neutrality suites bit-identical ⇒ the no-record world is provably unchanged; `git diff --stat -- Assets/Simulation` = PromotionManager.cs ONLY; **live godot-MCP boot clean — "gritty events 116 (dispatcher polling)", errors empty, day-9 save un-advanced.** **Review observations (non-blocking, disclosed):** (1) taking `dirt_going_straight`'s cheap exit while `compromised_syndicate` is active forecloses the buyout forever (its gate needs `in_the_life`) ⇒ the marker + its +365 shakedown persist with no remaining content clear — defensible ("leave cheap, the syndicate doesn't forget") but an ordering the spec's single-beat sketch didn't have; (2) §7.C's scratch-seeded live promotion smoke was skipped per the DIRT-2/DIRT-3 boot-only precedent — the ban/dock behavior is proven in the MC worlds instead; (3) the indictment pair re-offers every 45d while `in_the_life` stays active (spec-intended pacing; the door-out is the off-switch). **Deferred (unchanged from spec §10):** PED system completion (`ped_active` writer), tier-scaled salary the record could also dampen, avatar relegation-on-ban symmetry. **Next: user playtests; the arc has no further batches.**

- **2026-07-15 — DIRT-4 "The Bust & The Door Out" SPEC WRITTEN (Opus 4.8) — the arc FINALE:** `docs/design/criminal_underworld_dirt4_the_bust_and_the_door_out.md`. First arc batch that is **NOT calibration-inert** — user chose to fold the engine teeth in ("crime can take the diamonds away — permanently"). **Two halves ship together:** (A) **content** (Sonnet, inert) — the pro-scale bust as an OR-split plea that finally reads DIRT-2's `cooperated_with_league` hook (`dirt_the_indictment_a`=cooperated→lighter terms / `_b`=didn't→harsher), `dirt_the_record_follows` epilogue, `dirt_going_straight` clean-break-at-scale (clears `in_the_life`; optional expensive buyout clears `compromised_syndicate`; NEVER clears the record), + **consolidation edits** adding `set_flag record_arrest` to DIRT-1 `dirt_busted` + DIRT-3 `dirt_robbery_fallout` so one flag = "has a record"; (B) **engine** (Fable, calibration-TOUCHING) — `PromotionManager` avatar-eligibility branch reads the record via the already-wired indexed `BaseballQueries.LoadActiveFlagPlayerIds`: `banned_for_life`→promotion-ineligible (the terminal teeth DIRT-2 §8.1 promised), `record_arrest`→modest tunable `PromotionProfile.RecordArrestPenalty` dock to the avatar's `A`. Avatar-only read ⇒ NPC sweep byte-identical; inert until a record flag is set ⇒ neutrality guard (no-record world bit-identical, MLB PA/H/ER exact) is the load-bearing engine gate; MC batch re-runs. **Two grounding discoveries the memory/arc-docs lacked, both drove design decisions (user re-consulted):** (1) `record_arrest` already ships (set by `narcotics_arrest`) but is **read by nothing** — DIRT-4 makes it the canonical record; (2) **`ped_active` has NO gameplay writer** (only a MonteCarloHarness fixture sets it) — so the arc-docs' planned `caught_juicing`→`ped_active` re-gate was a **trap** that would strand the whole steroid arc (the four PED events actually fire off hustle-driven `detection_risk`, not juicing; the 1.5× PED multiplier is dead in live saves). **User ruling: leave PED events on `detection_risk`, no re-gate; optional prose re-theme toward league drug-testing; finishing the PED system = disclosed future arc.** **Next: build DIRT-4 — Sonnet 5 (content half + consolidation edits + harness checks) + Fable 5 (engine half + MC/neutrality gates, then reviews the content half). This closes the criminal arc.**

- **2026-07-15 — DIRT-3 "Product & Turf" (Opus 4.8 spec → Sonnet 5 build, `fbcb603`) — Fable 5 review SIGNED OFF, zero fixes:** 9 reactive events appended to `dirt_underworld_events.json` per spec `docs/design/criminal_underworld_dirt3_product_and_turf.md` — unlike the authored DIRT-1/2 cascades, every gate reads one of the seven `HustleService` engine flags (all seven names byte-verified against `HustleService.cs:209–296`; the spec's stale-name correction to `controls_turf_local` confirmed). All 9 slots match §5.2 exactly: turf trio (dispute w/ 3-day injury absence + NO authored rating debuff per the injury-rust rule; corner earns; give-up-the-corner clearing `controls_turf_local`), product trio (`bad_product`/`narc_watchlist` shared-read/`spoiled_goods`), robbery keystone (`dirt_robbery_fallout` closes the depth-pass §11 deferred arrest-absence seam: plea = 3-day arrest + clears `robbery_bust`, fight = 4-day + keeps it), `hot_goods`→fence nudge, card-room heat. Flag discipline pinned by harness: sets ONLY `in_the_life` (14 of 18 choices — the third on-ramp), clears ONLY the two disclosed engine flags, zero new content flags, cooldowns 9/9, no `detection_risk`, no `target:crew`. The §2-collision-2 SKILL.md allowlist companion edit landed in the same commit. Review verified mirror-fidelity of the convergence checks (mirror gates byte-match the real `dirt_family_finds_out`/`dirt_clean_break`/`dirt_made_useful` gates). Gates independently re-run: build 0/0; **GrittyEventsHarness 318→345/345** (count literals 101→110 at both pins, 27 new checks incl. fresh-save structural inertness at day 2 AND day 9999 against an empty flag set, `until_day` numeric proofs for all three absences, and the three-stage convergence cascade); calibration-inert by construction (commit touches nothing under `Assets/Simulation`); **live godot-MCP boot clean — "gritty events 110 (dispatcher polling)", errors empty, day-9 save left un-advanced.** Two review observations, neither blocking: (1) `dirt_sitting_on_hot_goods` has no `min_days_since` floor (deliberate, harness-pinned "a hot lot doesn't wait") ⇒ a player who robs successfully on day 1 gets a 0.10-weight roll ahead of `hs_first_day` on day 2 — but `hs_first_day` is flag-gated (`tier==0` + `!hs_started`), so worst case is a one-day onboarding delay, self-inflicted and fictionally coherent, NOT DIRT-1's sticky-fingers zero-action slot theft; (2) spec §7.6's live scratch-seeded cascade smoke test was not run (would require swapping the fixed appdata save + real-time clock driving) — the harness convergence checks exercise the identical dispatcher against real SQLite Worlds, matching the accepted DIRT-2 boot-only precedent; the user's hustle playtests will hit these beats organically. **Next: DIRT-4 spec = Opus 4.8; user playtests unchanged (jewelry run + hustle loops, which now also seed DIRT-3 beats).**

- **2026-07-15 — JewelryExchange 4th robbery target (Opus spec → build → Fable 5 review) — SIGNED OFF, zero fixes (auto-committer swept it into `d8c073b` mid-review):** spec `docs/design/hustles_robbery_high_stakes_target.md` — one new `RobberyTarget.JewelryExchange` = `TargetProfile(3600, 0.70, 15)`, one `JewelryButton` in `RobberyScreen`, 8 harness checks; nothing else touched (diff surface exactly spec §2). Key calibration ruling upheld: difficulty **0.70, not the pattern-following 0.80** — difficulty double-counts (Execute ×1.0 + Getaway ×0.5), and at 0.80 the careful cased-solo-bail line goes −EV (breaks the every-target-keeps-a-viable-careful-line invariant); at 0.70 it holds (+144.4 @ 55.4% bust empirical). Frontier extends clean: committed cased-solo-press **+469.7 @ 63.8%** (> bookie moderate 401.7 @ 38.3%); blind-strong-arm-press **−360.7 @ 87.0%** = the board's deepest trap. Gates: build 0/0; **HustleHarness 87→95/95**, no-regression proven properly — baseline run from a `git worktree` at `fbcb603` (NOT stash; auto-committer active) and an output diff showing the ONLY delta is the new jewelry block + tally, every shipped check line and printed band byte-identical; Baseball untouched by construction (MC skipped per spec §6.3); live godot-MCP boot clean on the day-9 save (errors empty ⇒ `JewelryButton` GetNode path resolves in `_Ready`). Side-findings: **R-4's hot_goods write side turned out ALREADY SHIPPED** (the pre-slice 87-check baseline carries the "robbery (R-4): ToResolution arms hot_goods" check ⇒ F-2's FreshFromAJob unlock path exists end-to-end — the roadmap's "next: R-4" pointer was stale), and **DIRT-3 (`fbcb603`) has NO Fable review on record yet.** Exits: user playtest (4th button renders, jewelry run end-to-end, bust bail = $900). **Next assignments: DIRT-3 review = Fable 5 (owed — Sonnet build, standing rule); further hustle content = Opus 4.8 spec first.**

- **2026-07-13 — DIRT-2 "The Fix" (Sonnet 5 build → Fable 5 review) — SIGNED OFF, zero fixes (auto-committer swept it into `2fd18f9` mid-review; its "22 new events" message counts the whole file — the batch is 11):** 11 events appended to `dirt_underworld_events.json` per spec §5's author's-call (`docs/design/criminal_underworld_dirt2_the_fix.md`): two ramps (`dirt_the_ask` / `dirt_made_useful` — a third `compromised_syndicate` writer converging into the shipped +365 shakedown), complicity ladder `on_the_hook`→`shaving_runs` (+aggravator `threw_a_game`), teeth (`dirt_league_investigation` survivable 20-game suspension / `dirt_permanent_ban` → terminal `banned_for_life` + season / `dirt_blacklisted` epilogue reader), doors out (`dirt_turn_informant_a/_b` OR-split → shipped enforcers; `dirt_walk_away_clean`, marker persists). Review verified all 11 slots against §5.2; one authoring delta approved (informant_a also gates `flag_inactive shaving_runs` ⇒ the OR variants are truly disjoint, header-disclosed); post-ban re-entry loophole hunted and confirmed CLOSED by construction (`compromised_syndicate` never cleared anywhere + `dirt_the_ask` blocks on `banned_for_life`); `served_suspension` 4th writer + `maturity` vocab both precedent-matched. Gates independently re-run: build 0/0, **GrittyEventsHarness 300→318/318** (count literal 90→101; new `RunDirtTheFixArcChecks` covers §7.3 incl. real-ConditionEvaluator tier safety at 0/1/−1 and the `until_day` numeric proofs), full 8-item `check_event_graph_integrity` script audit clean (zero orphaned prereqs / unpaced loops; dead-end sets = only disclosed forward hooks incl. `cooperated_with_league`; zero new contacts, all resolve), `Assets/Simulation` diff EMPTY (calibration-inert), **live boot via godot MCP clean — "gritty events 101 (dispatcher polling)", errors empty, day-9 save left un-advanced**. Batch is structurally inert at tier 0/1 ⇒ invisible in the current HS playtest until the avatar goes pro. **Next: DIRT-3 (fencing/turf) spec = Opus 4.8; deferred seams unchanged (`banned_for_life`→CareerManager wiring = future Fable engine task; `detection_risk`/`ped_active` shared-heat re-gate = DIRT-4; DIRT-1's `dirt_clean_break`-at-tier-0 polish still parked for a Sonnet slot).**

- **2026-07-13 — N-2 supplier haggle BUILT (Fable 5, uncommitted — `git log` first), all gates green:** the depth pass's §3.1/§10 fifth slice, the calibration-sensitive Lever-B front-stage. **Layer 1 (`NarcoticsHustle.cs`):** new `HaggleState` record struct + `HaggleOutcomeKind` (InProgress/Deal/Refused/Walked) + the parallel-method family per §11's resolved seam — `HaggleStart` (one RNG draw: hidden floor = `UnitCost × clamp(U[0.86,1.00] − 0.10·trust/100, 0.60, 1.05)`, trusted suppliers quote lower + get a 5th patience round at trust ≥ 50, opening ask = 1.4× floor), `HaggleCounter` (bid ≥ the descending live willingness closes at min(bid, ask); a lowball concedes the ask `DropFrac=0.5` of the way to the floor and burns a round; patience 0 = **Refused**: no run today + trust −4), `HaggleAccept`, `HaggleWalk` (no run, no consequence), `NeutralHaggleDecision` (accept at ≤ 0.85× opening ask — the buyer-side mirror of Fencing's `NeutralAcceptDecision`), all constants in `NarcoticsProfile`. A closed deal carries `DealUnitCost` + `BuyInMaxMult` (0.90–1.30 lerped by where the close landed: squeeze the floor = cheap units but a smaller front, pay the ask = 1.3× allotment — the margin/volume trade) + trust +1; `DropInventory` gained a `(unitCost, buyInMaxMult)` overload with the shipped 3-arg signature delegating at the flat 10/1.0 (shipped path byte-identical). **The LOCKED §3.1 EV-neutrality bind is harness-PROVEN:** the neutral autopilot closes deterministically at 1.1× floor ⇒ mean effective UnitCost **10.007** vs the shipped flat 10 (±0.15 band asserted), a patient haggler lands **9.55**, an eager accept pays **12.74** (skill-sensible both directions), and the Safe policy run end-to-end through a neutral haggle lands **92.8 vs the flat band's 93.9** (±8 assert) — variance and agency, not free money. **Layer 3 (`NarcoticsHustleScreen`):** new `HagglePanel` (ask label w/ patience readout, bid slider capped at the live ask, Counter/Accept/Walk) opens every run — re-ups re-haggle at trust nudged by the session accrual (`BeginRun` now folds `_accrued.SupplierTrustDelta` into the rebuilt ctx, the trust analogue of N-1's heat feed — disclosed: N-1 didn't nudge trust because nothing produced mid-session trust deltas until now); a Deal feeds the haggled cost/mult into the buy-in stage (deal terms echoed in the status label), Refused/Walked end the session with no run (forfeit-to-no-deal, the refusal's trust hit folding through the new resolution-shaped `FoldAccrued` overload — INV-1 intact, nothing hits the DB until Done); best-case-allotment affordability pre-gate keeps the shipped can't-afford panel reachable. **Gates: build 0/0 (full sln), HustleHarness 60→76/76** (16 new checks: haggle state-machine legality incl. absorbing terminals + never-charges-more-than-asked, trust-lowers-floor/buys-patience, the EV-neutrality band, the skill split, unit-cost + allotment plumbing through `DropInventory`, determinism, 0.0 B/run zero-alloc) — **every shipped band byte-identical** (Narcotics 93.9/225.5/1444.2, Fencing 225.5 @ 5.7% sting, Robbery 147.0/401.7/−40.4); `git diff --stat -- Assets/Simulation/Baseball` empty + **MC 345/345** run anyway per the standing rule; **live headless boot clean** (godot MCP absent this session — fell back to the toolchain file's `Godot_v4.7…_console.exe --headless --quit`: day-9 save loads, schema v13/WAL/FK, errors empty ⇒ Main's permanent-sibling `_Ready` resolved every new `HagglePanel` GetNode path, save untouched). Honest gate limit (N-1/R-2 precedent): no headless click-through of the live haggle loop — **exit: user playtest** (counter a few rounds and watch the ask fall + patience tick down, accept vs grind-to-refusal vs walk, deal terms echo into the buy-in line, a re-up re-haggles). **Next per §10: F-2 fencing acquisition (Sonnet 5 panel; `AcquireLot` math → Fable if it grows past a table lookup) → R-4 hot_goods cross-hook (Fable 5).**

- **2026-07-13 — F-1 fencing multi-lot loop (Sonnet 5 build → Fable 5 review) — SIGNED OFF, zero fixes (uncommitted at review time — `git log` first):** the depth pass's §4.2/§10 fourth slice, UI-only, `FencingScreen.tscn/.cs` only, zero Layer-1 touch. After a lot closes (Deal or Walk), "Show Him Another Lot" (flat `MaxLotsPerDay=3` per §11) rebuilds `FencingContext` from the session's base snapshot with Heat nudged by the accrued `DetectionRiskDelta` and re-calls `StartLot` — a hot lot's own sting exposure feeds the next lot's `ComputeStingProbability` (Lever-A tail scaling, no new risk math); `_accrued` folds every closed lot (`CombineAccrued`, N-1's accumulator byte-mirrored) until Done applies one summed resolution (INV-1) or a true no-op. **Review verified:** `FencingContext(heat, fenceStanding)` arg order matches the call; `Heat` = `detection_risk/100` so the `/100.0` nudge scale is exact; per-lot state isolation holds (fresh `StartLot` state, `FinishLot` the only fold point, terminal-only, resolver `RequireInProgress` backstops); Walk still folds Stress+8 + `spoiled_goods` as shipped; N-1's bust-fold-at-Done branch correctly has no Fencing analogue (every Fencing terminal passes through `FinishLot`). **Disclosed seam confirmed + deferred (user ruling pre-build):** §4.2's per-deal `FenceStanding` nudge needs a fence-standing delta field `HustleResolution` doesn't have (Fencing's apply path never touches the relationship graph) — future Layer-1/2 slice, Fable-adjacent, documented in the class doc. **Review closed the builder's disclosed gap:** godot MCP was available this session — live boot on the day-9 save, errors empty ⇒ the new `RunTallyLabel`/`ShowAnotherButton` `GetNode` paths resolve, no `_Ready()` NRE. Gates independently re-run: build 0/0, HustleHarness 60/60 (robbery/fencing bands byte-match R-1's recorded values). Non-blocking notes: `CombineAccrued`'s 11-arg ctor defaults the two bust flags (safe while Fencing never sets them, same latent shape as N-1); tail scaling is mild (one sting = +1pt next-lot sting probability) — playtest tuning question. **Exit: user playtest** (3 lots max, tally reads from lot 2+, walk and deal both re-offer, button gone at cap). **Next assignments: N-2 supplier haggle = Fable 5** (EV-neutral retune, §3.1 mean-UnitCost≈10 lock, calibration-sensitive) **→ F-2 fencing acquisition = Sonnet 5** (new pure `AcquireLot` is Layer-1-adjacent; if it grows math beyond a table lookup, reallocate to Fable) **→ R-4 hot_goods cross-hook = Fable 5** (additive `HustleResolution` field + `ApplyCore` branch, per the R-2 review note).

- **2026-07-12 — Sleep bands BUILT (Fable 5, user directive "sleep is an absolute"), all gates green:** planned nights get a **2h forced floor** (`SleepProfile.MinPlannedSleepHours`, forced in `SubmitDaySchedule` exactly like calendar blocks + slider `MinValue` re-derived from the live constant), **<6h = short night** (+8 stress, −1 happiness, on top of the proportional restore short blocks always had), **6–7h neutral**, **exactly 8h = the boost** (−4 stress, +1 happiness, full +80 restore), **>8h = oversleep** (new block-only `NpcActionId.Oversleep`: Sleep's 0.8 env, ZERO restore — harness-measured a 10h night ends *worse*-rested than 8h since the extra hours only decay off the cap — plus −1 happiness). All effects land in `TickScheduledDay`/`ApplySleepBand` ⇒ **avatar-only by construction** (only the avatar has plans; NPC autopilot Sleep untouched, byte-identity proven). ScheduleScreen: slider floor 2/default 8 + a 4-state `SleepHintLabel` band readout (danger/plain/plain/warning); tutorial step 6 teaches "8 hours — under 6 or over 8 and he pays". **Gates: build 0/0, NeedsDecay 109→117/117** (8 new checks: profile pins, Oversleep def pins, float-exact ±band stress deltas across fully-booked twin worlds, directional mood bands, the restore-cap proof, bystander byte-identity; **4 pre-existing person-layer fixtures deliberately migrated 8h→7h nights** — 8h stopped being neutral BY DESIGN, 7h is the new neutral band, each migration commented), **MC 345/345, Gritty 286/286, CoreLoop 22/22, Hustle 60/60**, live boot clean (day-8 save untouched). Exit: user plays — drag the sleep slider through the bands, confirm the hint states + a short-night stress hit lands. Constants first-pass, tunable via simulate_utility_decay.

- **2026-07-12 — Scene system: the center card is now ALWAYS populated (Fable 5, user directive "always have an image… wherever the player is"):** the two-scene LocationCard generalized into a full **scene resolver** (`BaseballDashboard.RefreshLocationCard`, priority: at-bat hides → **pending gritty choice** fronts `event_{category}` art via `gm.GrittyEventChoices.TryGetPendingChoice` → school window → game-day home park → **day-phase** home_night/home_morning/neighborhood/home_evening off exported minute boundaries 360/1080/1320). Every scene renders authored art if the file exists (per-school file → generic `Assets/Graphics/Scenes/{key}.png` → nothing), and when no art exists the card shows a **school-primary-tinted text placeholder** (`LocationPlaceholder` HeadingLabel) — the slot is never blank. School/Field scenes gated on the avatar's schools.json membership (college/pro tiers get event+day-phase only — future venue catalog). **`docs/design/scene_art_guide.md` = the user's authoring contract:** exact file list (6 schools × 3 + 14 generic scene files), sizing, the import gotcha, and the deferred ideas (side-game in the slot, per-event `image` field). Dirty key `(day<<8)|(scene<<4)|category`. Build 0/0, live boot clean on the user's new day-7 save (new avatar, relationships 60). Exit: user eye-gates the placeholder rotation through a day + authors scene art at leisure. **User confirmed the first slice live same session: "The image shows up!"**

- **2026-07-12 — School art rendering slice BUILT (Fable 5, user supplied CRW+LKS art):** schools.json already declared `images.logo/school/field` per school and `SchoolDefinition` parsed all three paths — nothing rendered them. Now: **new `SchoolArtLibrary`** (PortraitLibrary idiom, path-keyed cache, misses cached ⇒ the 6 unauthored schools cost one existence check and render nothing — art lands school-by-school with zero code churn); **TopBar** gains a 28px `SchoolLogo` TextureRect (avatar's school crest, tooltip = DisplayName, dirty on team-id change); **BaseballDashboard** gains `LocationCard` in the reorg's deliberately blank CenterSlot (the class doc's promised "contextual imagery"): school exterior during the school window (SchoolCalendar in-session day, clock 08:00+6h, `SchoolStartMinute` exported), else the **HOME team's field** on a scheduled game day (`TryGetScheduledGameFor` — away games show the opponent's park, caption "Game day — {away mascot} at {home school}"), hidden during a live at-bat / no art / non-HS tiers; dirty-keyed `(day<<2)|mode` so per-frame cost is arithmetic. **Gotcha closed: fresh PNGs had no `.import` sidecars (ResourceLoader-invisible) — `--headless --import` alone did NOT generate them on 4.7; `--headless -e --quit` did.** Build 0/0; no sim touch (pure UI reads of shipped public APIs). Exit: user picks Crestwood/Lakeside on a fresh save and eye-gates logo/school/field + the 6 art-less schools staying blank.

- **2026-07-12 — Household board slice BUILT end-to-end (Fable 5, user-approved from the meal-copy conversation) — spec `docs/design/household_board.md`, all gates green:** while the avatar is a **high-schooler with a Family_Background row** (FamilyService's exact parentalSupport population, HS-only — deliberately narrower than `AvatarSchoolAvailable`'s HS-or-College), the family covers a wealth-tier share of **board** = Eat's $12 FinancialCost + the $70/wk avatar-only cost-of-living bill, nothing else. One table (`HouseholdBoard.BoardShareByTier` = kid's own share: Destitute 1.0 / Working-class 0.5 / Middle+ 0.0); time is never covered (the Free-hours teaching survives). Mechanism: new `Assets/Simulation/Life/HouseholdBoard.cs`; `LifeSimManager.AvatarBoardWealthTier` (default **-1 ⇒ share 1.0 ⇒ byte-identity** for every pre-slice save + every harness world), set by `GameManager.SyncLifeSimAvatar` from the persisted `FamilyBackgroundRow.WealthTier`; the discount applies in BOTH the decision (`UtilityCalculator.SelectAction` optional `eatCostShare`, Eat row only — LegalWork's Hunger-primary income never scales) and the charge (`ApplyAction`, avatar detected via a `ReferenceEquals` on a `SetAvatar`-cached `_avatarRuntime` — no hot-loop string compare); cost-of-living debit ×share; `AvatarWeeklyCostOfLiving` exposed for UI. Bank tab line now 3-state (full / "your share $35/wk" / "covered by your family"), tutorial step-8 copy generalized, checklist line added. **Gates: build 0/0 (full sln), NeedsDecay 102→109/109** (7 new checks: table pins incl. −1 sentinel; the shipped broke-crisis fixture FLIPS to Eat at share 0 — a broke covered kid raids the fridge instead of picking a fight; one-forced-meal day debits −12/−6/0 exactly by tier; fully-booked week's bill −70/−35/0 exactly; bystander NPC fortnight byte-identical under tier 4 vs none), **MC 345/345, GrittyEvents 286/286, CoreLoop 22/22, Schema 111/111 (no DDL change), Hustle 60/60**, live boot clean (errors empty, day-3 save untouched). Live save's avatar = tier 2 Middle ⇒ user will see "covered by your family". Exit: user playtest. Out of scope (spec §6): NPC households, a "parents cut you off" narrative lever (trivial later — one tier/flag write), College partial support.

- **2026-07-12 — Tutorial "nobody eats" copy fix (Fable 5, user playtest confusion report):** step 6's "Book all 24 hours and nobody eats" read as a household/food-supply statement when the mechanic is *time*: the avatar eats automatically during Free hours (autonomy's Eat action, 1h + $12, `ActionCatalog.Eat`). Rewrote the step-6 body (`TutorialOverlay.cs`) to say meals are never scheduled — he handles them himself, but only in free time — and `ScheduleScreen.NoFreeHoursText` to "your player has no time to eat today"; checklist A2b quote synced. Grep confirms no .tscn overrides these exported strings. Build 0/0; strings only, zero sim touch. **Surfaced design suggestion (user): parents should cover food costs for a non-poor tier-0 kid; a poor kid has to find food money himself.** Assessed as a real slice, not a copy edit: `ActionCatalog` is shared by avatar + 819 NPCs, the Eat cost/cost-of-living economy is harness-calibrated (LegalWork's $25/hr is tuned against the autopilot spend) ⇒ needs an Opus spec ("household board": backstory-gated meal/cost-of-living coverage at tier 0) → Sonnet data edit → `simulate_utility_decay` re-run. Awaiting user's go. the Bank tab's NeedsCard turns a need's label danger-red at/under `NeedsEngine.CriticalThreshold` (20) with a 20-line tick per bar, but the main screen's TopBar needs bars had neither cue. Mirrored both in `TopBar.tscn`/`.cs` verbatim from the Bank-tab idiom: 5 scene-authored `CriticalMark` ColorRects re-anchored in `_Ready` from the live constant, plus a mask-dirty-flagged `RefreshCriticalNeeds` riding the existing needs identity gate (recolor only on a threshold crossing, no per-frame work). UI-only, zero `Assets/Simulation/` touch. Gates: build 0/0, live boot clean (errors empty, day-3 save untouched, clock Paused). Exit: user eye-gate — drop a need under 20 and confirm the TopBar label goes red like the Bank tab's.

- **2026-07-12 — N-1 narcotics re-up loop (Sonnet 5) — BUILT, UI-only, gates green:** the depth pass's §3.2/§10 second slice — biggest depth win for zero new resolver math. **`Assets/UI/NarcoticsHustleScreen.tscn`/`.cs` only** — after a run resolves (not on a bust), a new "Re-up & Run Again" button (capped at flat `MaxRunsPerDay = 3` per §11's resolved seam, in-memory counter) starts a fresh `NarcoticsHustle` run from the *live* accrued context instead of re-reading the DB: a new `_accrued` `HustleResolution` accumulator folds each run's terminal state (`CombineAccrued`), and `BeginRun` rebuilds `HustleContext` with funds/heat/crew-standing nudged by everything accrued so far — so a hot run's own detection delta feeds into the *next* drop's `pSeize` before a single byte hits the DB. This is exactly the "built-in tail scaling" the spec named: no new risk math, the shipped `Heat`-scaled seizure/bad-batch formulas do the work once heat accrues within the session. A bust ends the session outright (no re-up offered) and its losses fold into `_accrued` on Done rather than at bust-time, avoiding a double-fold. `OnDonePressed` applies the single summed `_accrued` resolution (INV-1: one net DB write for the whole multi-run session) or nothing at all if the accrual is a no-op (abandon mid-run still evaporates cleanly). New `RunTallyLabel`/`ReUpButton` nodes verified via a live Godot boot (clean `_Ready()`, no NRE on the new `GetNode&lt;T&gt;` paths) — a true click-through of the re-up loop itself needs a human or a scripted-input harness neither available headlessly here, flagged as the honest UI-only gate limit. **Gates: build 0/0, HustleHarness still 60/60 (zero Layer-1 files touched — `git diff --stat -- Assets/Simulation/` clean apart from R-1's already-reviewed `SetRobberyBustFlag`), Baseball diff empty so MC re-run not required by §9 and skipped.** Exit: user playtest (start narcotics, re-up 2-3x, confirm the tally label and the capped button, confirm a bust ends the session).

- **2026-07-13 — R-2/R-3 Fable 5 review — SIGNED OFF, zero fixes (slice swept into `3f3bdae` by the auto-committer mid-handoff):** the flagged Layer-2 crossing turned out spec-sanctioned — §5.3 enumerates `BuildRobberyContext`/`ApplyRobberyResolution`/`WorkActivity.Robbery`/the `SetRobberyBustFlag` writer as exactly the R-slice's Layer-2 surface, and §10 scoped R-2/R-3 as "Sonnet 5 → Fable review", which this is. Verified line-by-line: **crew-rep resolution** reuses `ResolveFactionRep(HustleCrewPlayerId)` + `RequireResolvedRep` + `AdjustOrCreateRelationship(..., Rival)` exactly as `ApplyNarcoticsResolution`'s crew path (relationship write after `ApplyCore`'s committed batch, same ordering); **`robbery_bust` wiring** in `ApplyCore` is the `gambling_bust` branch verbatim (inside the batch, before commit); **routing** proven end-to-end — ScheduleScreen's dropdown index==enum-value alignment holds for item 5, `SelectWorkActivity` casts generically, `GameManager.cs:1088`'s non-LegalWork branch arms Robbery with zero new code, BurnerPhone quick-launch preselects the planner (parity, not an ad-hoc launch — §8's rejected-decoupled-launch ruling intact); **screen** is the Narcotics pattern verbatim (same `_Process` pending-gate, forfeit-evaporates on day-advance even from the un-Done result panel = INV-1, same TickCount RNG idiom, all 24 `GetNode` paths byte-match the .tscn). Gates independently re-run: **build 0/0, HustleHarness 60/60** (robbery band unchanged: cautious +147 @ 6.0% / moderate +402 @ 38.3% / reckless −40 @ 80.9%), Baseball diff empty by commit-stat inspection. **Non-blocking notes for later slices:** (1) `RobberyState.HotGoodsFlag` accrues but `ToResolution()` drops it — correct for now (R-4's scope) but wiring it will need an additive `HustleResolution` field + `ApplyCore` branch, i.e. a Fable-adjacent touch even though §10 lists R-4 as Sonnet; (2) `BuildRobberyContext` hard-codes `hasCrew: true` (rep resolution succeeds or throws), so the screen's crew-disabled path is dead code today — harmless, defensive; (3) polish candidates: surface `ComputeExecuteSuccessProbability`/`ComputePressLuckBustProbability` as on-screen odds (the resolver already exposes them publicly), and the Grabbed label shows the pre-split take on crew jobs (split lands at bank time, documented resolver semantics). Remaining exit: **user playtest** (all three approaches, bust bail-fee vs clean-getaway messages, the press-your-luck beat). Next per §10: **F-1 fencing multi-lot (Sonnet 5)**.

- **2026-07-13 — R-2/R-3 robbery screen + press-your-luck beat (Sonnet 5) — BUILT, all gates green, awaiting Fable review:** the depth pass's third slice (§10), making R-1's pure resolver playable. **New `Assets/UI/RobberyScreen.tscn`/`.cs`** — a fifth permanent Main.tscn sibling (Scrim+Panel idiom verbatim from Narcotics/Fencing) driving the four-stage sequence: Case (3 mark buttons + a "case it" checkbox) → Approach (solo-quiet/strong-arm/crew, crew button disabled when `!ctx.HasCrew`) → Execute ("Go" rolls the resolver) → **Grabbed** (the press-your-luck live beat: "Press Your Luck" vs "Grab and Run") → Getaway → Result/Done, entirely in-memory until `OnDonePressed` (INV-1, same abandon-evaporates forfeit as the other three screens). **Layer 2:** `HustleService` gains `WorkActivity.Robbery` (fifth enum member — zero GameManager arming changes needed, confirmed at `GameManager.cs:1088`'s generic "any non-LegalWork" branch), `BuildRobberyContext` (resolves the shared `HustleCrewPlayerId` rep, same lazy-resolve-and-cache pattern as Narcotics/Fencing), and `ApplyRobberyResolution` (shared `ApplyCore` primitives + the crew-job `CrewStandingDelta` adversarial-edge write, mirroring Narcotics' crew-standing path); `ApplyCore` now also writes the `robbery_bust` flag on `SetRobberyBustFlag` (was defined on `HustleResolution` by R-1 but never wired to a writer until now). **Planner/Bank-tab surface:** `ScheduleScreen.tscn`'s `WorkActivityOption` gains a 5th "Robbery" item; `BurnerPhone` gains a `RobberyButton` in the Bank tab's `HustleButtonsRow` (parity with the other three hustles' quick-launch buttons, wired through the existing `HustleLaunchRequested` signal → `Main.OnHustleLaunchRequested` bridge — zero Main.cs changes needed, the cast is already generic over the enum). **Gates: build 0/0 (full sln), `HustleHarness` still 60/60** (zero Layer-1 resolver files touched — R-1's math is untouched, this slice only calls it), **`git diff --stat -- Assets/Simulation/Baseball` empty so the MC re-run is inert-by-construction and skipped per §9(b)** (same precedent as N-1), **live boot clean** (godot MCP: day-9 save loaded intact, errors array empty through boot + a clock-persist tick, confirming `RobberyScreen._Ready()`'s new `GetNode<T>` paths and `BurnerPhone`'s new `RobberyButton` lookup both resolve without NRE). **Not yet reviewed:** §10 assigns Fable 5 review to every Sonnet sub-slice, and this one crosses into Layer 2 (not pure UI) — flagged for review before being considered signed off. A true click-through of the live press-your-luck decision needs a human or scripted-input harness, neither available headlessly — the honest UI-only gate limit, same disclosure as N-1. Exit: Fable review, then user playtest (run all three approaches, confirm a crew-less save disables the Crew button, confirm a bust shows the bail-fee message and a clean run shows the resolved message).

- **2026-07-12 — R-1 robbery pure resolver + `HustleHarness` band (Fable 5) — BUILT, all gates green:** the depth pass's first slice, the one true greenfield. **New `Assets/Simulation/Hustles/RobberyHustle.cs`** — pure Layer-1 resolver in the established shape (`RobberyContext` readonly struct: Heat/Reck/`HasCrew`; `RobberyState` record struct with `with`-copy accrual; every constant in `RobberyProfile` + per-enum `TargetProfile`/`ApproachProfile` tables; `RngState`-only, engine/Data-free). Four locked §5.1 stages as one-method-per-transition: `CaseTarget` (3 marks store/bookie/warehouse, optional casing beat = small heat cost now for a better execute roll) → `ChooseApproach` (solo-quiet/strong-arm/crew ×1.0/1.35/1.7 score, crew gated on `HasCrew` + 60% split + standing nudge, strong-arm risks `health_ceiling` on a bust) → `Execute` (success secures the 0.5 partial and lands the **Grabbed press-your-luck beat**: `PressLuck` for the full score at a rising bust roll vs `GrabAndRun`, both → new `Escaping` stage) → `Getaway` (botch roll lowered by casing + quiet approach — the early picks visibly pay off; clean banks + arms `HotGoodsFlag`, botch converts the take ×0.5 + heat, never itself a bust). Busts write the §5.2 bundle: takes wiped, **negative `FundsDelta` (bail/legal fees — user ruling 2026-07-12: `BustLegalCostFrac` 0.25 × mark base score, clamped to `RobberyContext.Funds` so it zeroes out but never indebts)**, `BustDetectionSpike` 12 toward the arrest triad, stress 25, `RobberyBustFlag`. **One additive `HustleResolution` field (`SetRobberyBustFlag`, defaulted — the `SetGamblingBustFlag` precedent verbatim); no schema change; Layer 2/3 untouched (R-2's slice).** Neutral autopilot per §5.3 = `NeutralPressLuckDecision` ⇒ never press (cautious stand-in). **Gates: `HustleHarness` 58/58** (26 new robbery checks: state-machine legality incl. absorbing terminals + crew-without-rep throw, per-stage fixtures, skill monotonicity incl. heat-hurts-everything, determinism, 0.0 B/run zero-alloc) — **shipped Narcotics/Fencing bands byte-identical** (safe 93.9/moderate 225.5/aggressive 1444.2; fencing 225.5/5.7% sting); **robbery EV/tail band skill-sensible per §9(c), harness 60/60 after the stake ruling:** cautious (store, cased, quiet, bail) mean **+147** @ **6.0%** bust; moderate (bookie, cased, quiet, press) **+402** @ **38.3%**; reckless (warehouse, blind, strong-arm, press) mean **−40** @ **80.9%** bust — INV-2 strict ordering proven AND the reckless tail eats its own upside (a first-build "no stake ⇒ FundsDelta ≥ 0" seam was flagged and the user immediately ruled it closed: busts must cost money; ruling recorded in spec §5.2). Build 0/0 ×2, `git diff -- Assets/Simulation/Baseball` empty, **MC 345/345**. **Next per §10: N-1 narcotics re-up loop (Sonnet 5, no new math) → R-2/R-3 robbery screen + press-your-luck beat (Sonnet 5 → Fable 5 review).**

- **2026-07-12 — Hustle mini-games DEPTH pass spec (Opus 4.8) — DONE, contract LOCKED:** `docs/design/hustle_minigames_depth_pass.md`. **Reconciliation up front (§0): the roadmap's "hustle minigames unbuilt" premise is wrong** — grounded against real code, **Texas Hold'em (deep, full NLHE table), Narcotics (3-stage resolver), and Fencing (alternating-offer) all ALREADY SHIP wired** (Layer-1 pure resolvers in `Assets/Simulation/Hustles/` + Layer-3 screens `TexasHoldemTable`/`NarcoticsHustleScreen`/`FencingScreen` + `WorkActivity` planner dropdown + `HustleService` orchestration + `HustleHarness`). Only **Robberies** is greenfield. `GAME_IMPROVEMENTS.md` lines 54–56 ask to make dealing/fencing "progressive mini-games with stages" and robberies "more interactive" ⇒ this is a **depth pass on shipped calibrated cores + one net-new game**, not four builds (user picked "deepen all + add robberies"). Design: two levers — **A** multi-round sessions (keep-going-or-bank, heat accrues in-memory ⇒ free tail scaling) + **B** front-stages (acquisition/casing/haggle). Narcotics gets re-up loop (N-1, no new math) + supplier haggle (N-2); Fencing gets multi-lot (F-1) + acquire-the-lot front-stage (F-2); **Robberies** = net-new 4-stage game (case→approach→execute-with-press-your-luck→getaway) feeding Fencing via a `hot_goods` flag so the 4 hustles become one economy; Hold'em = presentation-only, deferred (user's list omits it, it's already deepest). Two hard invariants preserved: **INV-1** one net `HustleResolution`/armed day, accrue-in-memory, forfeit-clean; **INV-2** upside always buys career tail-risk. No schema change, no new resolution shape (at most one additive `SetRobberyBustFlag`), wall intact, Work-block-armed session model unchanged. **Continued on recommendations: §5.1 robbery stages LOCKED, §3.1 haggle EV-neutrality target LOCKED (haggle adds variance+agency, NOT free money — mean effective UnitCost ≈ shipped flat 10), §11 seams resolved to defaults** (parallel `HaggleSupply` method not shared kernel; flat N=3 re-up cap; `hot_goods` consume-on-use; robbery arrest-teeth deferred to DIRT). Zero code/schema touch. **Next: Fable 5 builds R-1** (robbery pure resolver + `RobberyContext`/`RobberyState` + neutral autopilot + `HustleHarness` EV/tail band) against the frozen contract → Fable review; then N-1/R-2-R-3/F-1 per §10 order.
- **2026-07-11 — Prominence pass P-1/P-2/P-3 review SIGNED OFF with one fix + the placement call (Fable 5, uncommitted — `git log` first):** verified the entry below against `event_choice_prominence_pass.md` §4/§5/§8. Sound throughout: `RefreshDecisionAttentionCues` rides the existing `_Process` identity gate *after* the `unchanged` short-circuit (fires on rise, identity swap, AND fall — no per-frame `SetTabTitle`/format work), §5 honored (banner press is the only tab switch), badge round-trips via the `_Ready`-captured `GetTabTitle`, `AccentButton` + `UiColors.Danger` are both real theme vocabulary, the time-bar override sits inside the `face != _shownFace` dirty block, the card's `TextMessage is not null` guard matches the applier's own convention, `ContactId` is crash-safe on unknowns (registry's `"unknown"` fallback row), and both surviving `BuildEventCard` callers pass non-null `resolutionLine` post-split. **Review fix — `PortraitView` pre-tree NRE (P-3, would have crashed the first real fire):** `BuildPendingEventCard` calls `SetIdentity` on a freshly `Instantiate()`d portrait while the card is still unparented — `_Ready` hasn't run, so `_textureRect`/`_initialsLabel` are null, and since `PortraitLibrary.TryLoad` misses for every identity today the crashing fallback branch was the *guaranteed* path the moment a pending avatar card actually rendered (invisible to Sonnet's boot, which deliberately staged no fire). Fixed component-level in `PortraitView.cs`: `SetIdentity` records key+name and defers the visual apply behind `IsNodeReady()`; `_Ready` re-applies any pre-tree identity — in-tree callers (`_threadPortrait`, dashboard avatar) byte-identical behavior, any future pre-tree caller safe, contract documented in the class doc. **Placement call — APPROVED, Sonnet's phone-internal home preferred over BOTH spec sketches:** `Screen/ScreenLayout/AttentionBanner` keeps §4.3's cross-tab reach (above the TabContainer ⇒ visible on all eight tabs), reads diegetically as a phone notification rather than a system strip, and needs zero Main.cs bridging — the spec's "preferred" Main-level bar would have required a cross-scene signal hop to switch the phone's tab (ui_conventions forbids tree-reaching), and the dashboard half keeps its cue via the §4.4 red face anyway. Ruling recorded in spec §10 (seam RESOLVED). Gates re-run post-fix: **build 0/0** (8 projects), **GrittyEvents 286/286**, **MC 345/345**, `git diff --stat -- Assets/Simulation Assets/Narrative` **both empty**, live boot (godot MCP) clean on the real day-3 save (schema v13/WAL/FK, 90 events polling, errors empty, save untouched). One flake note: a concurrent background MC re-run aborted exit-255 mid-boot (PS 5.1 `2>&1` NativeCommandError artifact + build-lock overlap with the booting game) — the foreground post-fix run is the binding 345/345; nothing stochastic reappeared. **Remaining exit: user eye-gate** — the pending-card render path is build+review-proven but not live-driven (no input injection via godot MCP; force-advancing the real save to stage a fire would mutate it). **Next models: user playtest (eye-gate) → then Opus specs the next big-gameplay slice (hustle minigames or DIRT-2, user picks).**
- **2026-07-11 — Event-choice prominence pass P-1/P-2/P-3 BUILT (Sonnet 5, uncommitted — `git log` first):** per `docs/design/event_choice_prominence_pass.md` (§5 locked same session: badge + banner, no auto-switch). **P-1:** `TimeControlBar.RefreshControls` colors `_statusLabel` with `UiColors.Danger` when `face == BarFace.DecisionNeeded` (cleared otherwise) — a few lines in the existing face-switch, no new node. **P-2:** new `AttentionBanner` Button in `BurnerPhone.tscn` (`Screen/ScreenLayout`, between StatusBar and PhoneTabs — NOT a Main-level sibling as the spec's "preferred" option sketched; placing it inside BurnerPhone's own ScreenLayout means it's visible above every tab with zero Main.cs bridging, since BurnerPhone already owns `Contacts`/`_phoneTabs`/the pending-choice poll — a simpler realization of the same requirement, disclosed for Fable's review), `AccentButton` themed, `Visible`/`Text` driven by a new `RefreshDecisionAttentionCues` call riding the existing `_Process` identity-changed gate (no new per-frame work); press → `_phoneTabs.CurrentTab = _eventsTabIndex` (the ONLY tab switch, always player-initiated per the locked §5). Same method also drives the Events tab's `•` badge via `SetTabTitle` (original title captured once in `_Ready` via `GetTabTitle`, so it round-trips cleanly). **P-3:** new `BuildPendingEventCard` (split out of `BuildEventCard`, which now only renders resolved/history cards — its `resolutionLine` param was always non-null after the split, so the dead null-branch was removed too) — adds an identity row (runtime-instantiated `PortraitView` via a new `[Export] PortraitViewScene` field wired to the existing `2_portraitview` ext_resource, + contact `DisplayName`) and, when the firing event authored one, the `TextMessage` companion line — confirmed non-redundant with Messages: `EventConsequenceApplier` only writes `TextMessage` to the narrative log inside `ApplyChoice`, which hasn't run yet while a choice is pending, so the live card is the ONLY place the player sees it before deciding. Gates: **build 0/0** (full solution, 8 projects), `git diff --stat -- Assets/Simulation` **empty**, **GrittyEventsHarness 286/286** (pause/resolve/forfeit section unchanged — this slice only reads that seam), **MonteCarloHarness 345/345** byte-exact. **Live boot (godot MCP) on the real save clean:** schema v13/WAL/FK, day 3, 48 teams, 819 NPCs, 90 gritty events, UiSfx ready, errors empty, save untouched (day 3, clock Paused — deliberately did NOT force-advance to trigger a fire, since driving the live save forward to inspect it would consume/mutate it; the boot only proves every new `GetNode`/`GetTabTitle` path resolved without throwing). **Disclosed, NOT built:** P-4 (audio `UiSound.Alert` + motion) — parked per the user's "away from sounds" directive, per spec. **The banner's placement (BurnerPhone-local vs the spec's Main-level sketch) is the one design deviation for Fable's review to weigh in on** — functionally equivalent, architecturally simpler, no cross-scene reach. **Next: Fable 5 review (gate re-run + live boot + the placement call) → user's eye-gate playtest** (does an owed decision now feel unmissable).
- **2026-07-11 — Event-choice UI: roadmap correction + prominence spec (Opus 4.8):** Grounding the queued "avatar event-choice UI (Opus spec first)" slice against real code revealed it **already ships** — `GameManager.cs:591` sets `AutopilotAvatarChoices=false`, avatar fires pause as `PendingGrittyChoice` (`EventConsequenceApplier.cs:170`), `CanAdvanceDay`/`TimeControlBar` gate on `HasPendingChoice`, the phone Events tab renders the pending card + `ReplyChip`→`ResolveChoice(i)` (`BurnerPhone.cs:772`); GrittyEventsHarness has the pause/resolve/forfeit section, `AchievementManager` consumes `GrittyEventResolvedEvent`, and the content model (`EventChoice.AutopilotWeight`/`Label`/`Prompt`/`Outcome`) was authored *for* this UI. Surfaced the contradiction to the user; user chose the **prominence/immersion pass**. Wrote `docs/design/event_choice_prominence_pass.md`: amplify attention *to* the phone (no modal — `EventChoiceScreen` was deliberately retired Phase 10b), keyed off the phone's existing rising-edge poll (no new bus wiring). Mechanisms: §4.2 Events-tab `•` badge (`GetIndex()`/`SetTabTitle` idiom), §4.3 non-blocking shell banner "{Contact} needs an answer" → tap focuses Events tab, §4.4 louder `DecisionNeeded` time-bar face (UiColors danger), §4.5 richer live card (PortraitView + contact name + `TextMessage` companion line). Sub-slices P-1/P-2/P-3 each bootable; P-4 (audio `UiSound.Alert` + motion) deferred per the "away from sounds" directive. **§5 = the one user decision surfaced: auto-switch to Events tab on fire vs badge+banner — recommend badge+banner (agency).** Zero sim/schema/content touch; no new harness (live boot + eye-gate). Build = **Sonnet 5** → **Fable 5** review. No code touched this session.
- **2026-07-11 — Priority pivot (Fable 5, user-directed):** reviewed the refreshed `GAME_IMPROVEMENTS.md` against shipped state. User directive: "get away from sounds and little things" ⇒ D-2/D-3 audio wiring, Slice E theming, and Slice F animations deprioritized to the back of the queue (D-1 stands, auto-committed as `c0b1d62`). **Next slice chosen by the user: the avatar event-choice UI (Opus spec first)** — turns autopilot-resolved gritty/dirt events into player choices, unblocking the doc's top "more interactive story" asks and every future DIRT batch. After it: hustle minigames (Hold'em full game, narcotics stages, fencing, robberies) and DIRT-2. Roadmap rows 1/4/5 demoted, row 6's §9 deferral is now the active slot. No code touched this session.
- **2026-07-11 — Slice D-1 BUILT (Fable 5, UNCOMMITTED):** the audio service per `ui_audio_slice_d.md` §3/§5/§6/§7.1. New `Assets/UI/UiSfx.cs` — second autoload (registered after GameManager in `project.godot`), `Instance` + `ProcessMode.Always` idiom, 8-voice pooled `AudioStreamPlayer` round-robin on the new `SFX` bus (zero-alloc play path: table lookup + int cursor + `Play()`), `UiSound` 8-role enum→stream table, all placeholder tones **synthesized at boot into `AudioStreamWav`** (mono 16-bit 44.1k, 5 ms attack + exp decay, one distinct timbre per role — zero committed assets; note the Godot C# type is `AudioStreamWav`, not the spec's `AudioStreamWAV`). New root `default_bus_layout.tres` (`Master`→`SFX`) — picked up natively (boot log shows bus index 1, no fallback warning); `_Ready` carries a create-in-code guard if the layout ever goes missing. **§3.5 built on the recommended Option A default** (`user://settings.cfg` `[audio] sfx_volume`; user had not yet replied — flip to KV is ~5 lines in `LoadVolume`/`SaveVolume` if vetoed); mute-at-0 edge per §3.2; a `_volumeDirty` flush in `UiSfx._ExitTree` covers keyboard-driven slider changes that never emit `DragEnded`. Slider un-parked (`BurnerPhone.tscn` editable, note label repurposed to "Sound effects volume."; `BurnerPhone.cs` seeds via `SetValueNoSignal`, `ValueChanged`→live `SetVolume`, `DragEnded`→`SaveVolume` + preview Tap, disconnected in `_ExitTree`). NO game-wide wiring yet — D-2's auto-wire and D-3's stings are deliberately not started (§7's thin-slice order). Gates: build 0/0 (game + all 7 harness projects), `git diff --stat -- Assets/Simulation` EMPTY ⇒ MC inert by construction, no schema touch (ConfigFile ≠ DB) ⇒ SchemaValidator unchanged, live boot (godot MCP) on the real save clean — **"UiSfx ready: 8-voice pool on 'SFX' bus (index 1), volume 100%"**, errors empty, save untouched (day 3, clock Paused), Steam-DLL notice known-benign. Disclosed seam: audibility + the quit-relaunch volume restore are the user's-ear gates (godot MCP can't hear or drag). **Next: user playtests D-1 (slider live level, release blip, 0 mutes, relaunch restores) + may veto §3.5; then D-2 auto-wire + D-3 stings (Fable, or Sonnet per spec §10).**
- **2026-07-11 — Slice D UI-audio design doc (Opus 4.8) — DONE + UNBLOCKED (one confirm):** `docs/design/ui_audio_slice_d.md`. Greenfield confirmed by grep (zero `AudioStreamPlayer`/`AudioServer`/`.ogg`/`.wav`, no `default_bus_layout.tres` — ships silent on implicit Master). Design: a **second autoload `UiSfx`** (in `Assets/UI/` — engine-coupled, must stay out of CoreLoopHarness's Core-compile scan; GameManager's `Instance` + `ProcessMode.Always` idiom, so it plays through the at-bat/menu tree freeze); a minimal `default_bus_layout.tres` (`Master`→`SFX`, slider drives SFX via `LinearToDb` with a mute-at-0 edge); an **8-voice pooled `AudioStreamPlayer` round-robin, zero-alloc play path** (streams pre-synthesized once, cursor is an int — the ui_conventions pool mandate applied to audio); a small **`UiSound` enum → `AudioStream[]` vocabulary** (Tap/Confirm/Back/Error/TabSwitch/Cash/Alert/DayTick) that is the stable contract — **placeholder tones are synthesized in C# into `AudioStreamWAV` at boot (zero committed assets, nothing for Phase-9 export filters)** and real recorded assets swap into the same table with zero call-site churn. **Wiring split to stay tractable:** auto-wire the default `Tap`/`TabSwitch` from ONE `SceneTree.NodeAdded` hook (every button/tab sounds, `"silent_ui"` group opts out) + a *small curated* set of hand-placed semantic stings (Confirm/Error/Cash/Alert/Back/DayTick) — not a 200-site sweep. Un-parks Slice B's `VolumeSlider` (`BurnerPhone.tscn:717` editable=false → true, wire `ValueChanged`→`UiSfx.SetVolume`, delete the "Sound arrives later" note). **The one decision surfaced for the user (§3.5): volume persistence home — recommend `user://settings.cfg` `ConfigFile` (correct per-device semantics + total DB decoupling) over Game_State KV; this deliberately DIVERGES from Slice G (the clock's minute IS per-save world state; volume is a device preference that must survive a save wipe/new-game).** Sub-slices D-1→D-4, each bootable. No `Simulation/` touch ⇒ MC inert; no schema change ⇒ SchemaValidator green unchanged; no new harness (audio = pure UX, tripwire is live boot + the ear). **Model assignments: D-1 = Fable 5 (allocation-sensitive pool + synth foundation, roadmap-tagged); D-2/D-3 = Fable 5, or Sonnet 5 if reallocating (pure wiring, no zero-GC subtlety, no harness); D-4 deferred. Next: user confirms §3.5, then Fable builds D-1.**
- **2026-07-11 — DIRT-1 review SIGNED OFF with one review fix (Fable 5):** the entry below's "UNCOMMITTED" label is stale — the auto-committer swept the whole batch into `a4a01c3` (recycled message). Verified against `criminal_underworld_content_arc.md` §4–§7: all 11 events avatar-scope, §5.1 flag lifecycle exact, `detection_risk` untouched, every `person_stat` name in the parser's closed switch, `hs_started` writer real, `the_bookie` row + registry header done, and the three World proofs sound (the disclosed `marker_mirror` gate is a legit test-fixture-only fix). **Review fix — day-2 slot steal (doc §5.3 violated):** `dirt_sticky_fingers`'s bare `funds<200` gate holds on day 2 for the Destitute backstory ($50 floor, 12% of rolls), and `dirt_…json` sorts alphabetically BEFORE `hs_onboarding_…json` while the dispatcher fires the first satisfied definition in file order — a weight-0.08 roll at stealing the avatar's day-2 slot from the weight-1.0 `hs_first_day` (≈1% of fresh saves, and it would flake checklist A2's pinned day-2 beat). Fixed with an `hs_started`+14d anchor (`dirt_lookout`'s own belt-and-braces idiom, disclosed in the batch header) + a new harness check running the real `ConditionEvaluator` over every DIRT-1 gate against a worst-case Destitute day-2 subject — pins the class, not just the instance. **Disclosed, not fixed:** `dirt_clean_break` ships on `coach_reyes` (doc-sanctioned) yet is fully reachable at tier 0, where every previously shipped coach_reyes event is age/flag-gated out — an HS save can receive the pro coach's thread; DIRT-2-era polish candidate (per-tier split or `family_mom`). Gates: build 0/0, GrittyEvents **286/286** (90 events), `git diff --stat -- Assets/Simulation` empty, live boot green ("gritty events 90 (dispatcher polling)", schema v13/WAL/FK, errors empty, clock restored Paused, save un-advanced — it reads day 3: the user has played since the day-2 note). **Next: user plays (dirt beats autopilot-resolve into the phone over longer sessions); next build slot = Slice D audio (Opus spec → Fable), DIRT-2 spec (Opus), or the avatar event-choice UI spec (Opus) — user picks.**
- **2026-07-11 — DIRT-1 "The On-Ramp" AUTHORED (Sonnet 5, UNCOMMITTED), per Opus 4.8's `docs/design/criminal_underworld_content_arc.md` §5/§6/§7.3:** New `Assets/Narrative/Events/Content/dirt_underworld_events.json` — 11 `scope:avatar` `hustle`/`family` events implementing the gambling-debt spiral exactly to the §5.1 flag-lifecycle table (`owes_the_book`→`dodging_debt`/`running_errands`→**`in_the_life`**, converging into the shipped `compromised_syndicate`→`syndicate_shakedown` spine via `dirt_debt_sold`), plus petty-crime texture (`dirt_lookout`, `dirt_sticky_fingers`→`dirt_busted` arrest absence), fallout (`dirt_family_finds_out`, `dirt_hot_merch`, `dirt_street_rep`), and the `dirt_clean_break` exit. `detection_risk` never touched anywhere in the batch (§2 collision #1 honored). Added test-only `cooldown_days`/`min_days_since` beyond the doc's explicit numbers on 6 slots whose every choice left their own gate re-satisfiable (`dirt_the_marker`, `dirt_lookout`, `dirt_busted`, `dirt_family_finds_out`, `dirt_hot_merch`, `dirt_street_rep`, `dirt_clean_break`) — disclosed in the batch's own header comment, no `check_event_graph_integrity` #4 violations. `the_bookie` ("Sal", role `friend`) added to `contacts.json` + its `//` header, zero `ContactRegistry.cs` touch. `GrittyEventsHarness` gained `RunDirtUnderworldArcChecks` (§7.3): static pins against the real parsed file (flag lifecycle choice-by-choice, avatar scope, no orphaned flags, no `detection_risk`), plus three dynamic `World` proofs using deterministic weight-1.0 mirrors (the established `RunCascadeAndWriterChecks` technique, needed since the real weights are <1 and the +365 timing claims need exact-day arithmetic) — **both on-ramps converge on `syndicate_shakedown` at exactly +365** (bribe path day 2→367; debt path `chase_the_pot`→`duck_the_call`→`dirt_debt_sold` day 2→12→42→407), and **the bust benches** (`dirt_busted`'s arrest absence computes `until_day` correctly). Whole-folder count pins bumped 79→90 in both `RunContentChecks` and `RunContactRegistryChecks`. **One self-caught bug during the harness build:** the debt-path mirror's `marker_mirror` had no gate against re-firing after its first "duck," so it refired every day for 395 days, continuously resetting `dodging_debt`'s clock and starving `dirt_debt_sold`'s `min_days_since:30` window — fixed by adding a test-only `flag_inactive:dodging_debt` gate (a single-choice deterministic mirror always re-picks the same branch, unlike the real content's weighted 3-choice autopilot draw, which converges away from repeat-ducking almost every real playthrough — so this is a test-fixture-only fix, not a real-content bug). Gates: `dotnet build` on the full solution **0 warnings/0 errors**, `GrittyEventsHarness` **285/285**, `git diff --stat -- Assets/Simulation` empty (calibration-inert by construction, no MC re-run mandated). **Not yet done:** the live godot-MCP boot check (§7 gate 5) — left for Fable's review pass. **Next: Fable 5 reviews against the design doc + re-runs §7's gate set + live boot, sign-off.**
- **2026-07-11 — Content arc STARTED: "The Dirt" criminal-underworld spec (Opus 4.8):** user picked the criminal arc to lead the content push (over College-bridge / pro-career-depth). Wrote `docs/design/criminal_underworld_content_arc.md`: a multi-batch spine (DIRT-1 on-ramp → DIRT-2 game-fixing @tier≥2 → DIRT-3 fencing/turf → DIRT-4 bust & going straight), a reusable **authoring contract** (§4), and **DIRT-1 "The On-Ramp" in full buildable detail** (§5) — ~11 `scope:avatar` `hustle` events, a gambling-debt spiral whose ducked-debt branch **converges into the shipped `compromised_syndicate` spine** (so `syndicate_shakedown@+365` becomes reachable by a 2nd on-ramp), `in_the_life` master flag, a `dirt_busted` arrest-absence for real bench-time teeth, and a `dirt_clean_break` exit. Two shipped collisions caught + routed: (1) `caught_juicing` gates on `detection_risk>70` with **no PED-use check**, so criminal content uses **boolean jeopardy flags, never `detection_risk`** (re-gate on `ped_active` deferred to DIRT-4); (2) syndicate stays anonymous on `unknown`, one named associate `the_bookie` ships on existing role `friend` (pure content, zero `ContactRegistry.cs` touch). Grounding: read all 9 content batches + the full parser/evaluator/applier + tier-ladder + integrity SKILL — confirmed the closed vocabulary (Sonnet can't add consequence types), the person_stats-are-write-only rule, the NULL-team tier-leak (avatar scope is immune), and the `interest`-no-op-on-avatar constraint, all folded into §4. Zero code/schema touch. **Next: Sonnet 5 authors `dirt_underworld_events.json` + `the_bookie` row + §7.3 harness checks/count pin → Fable 5 reviews + gates (§7).**
- **2026-07-11 — Onboarding-2 T-2 review SIGNED OFF with two review fixes (Fable 5):** Verified against the overlay doc §3/§5/§7: resume math correct (KV 1-based, `step - 1` at the one `Main.TryOpenTutorial` boundary — Sonnet's self-caught off-by-one fix confirmed; absent-key→0→clamp lands step 1), §3.3 save-compat guard exact (`absent && current_day > 2` ⇒ -1), all six of Main's resolver paths re-verified against the live .tscn trees (TimeControlBar, PhoneTabs, NeedsCard, FundsCard, SaveCard, shell), step copy verbatim mod stripped markdown, signals connected/disconnected per conventions, phone tab-index logic reorder-safe (`GetIndex()`-derived). **Review fix 1 — the overlay was an input wall:** root `mouse_filter` was Stop, but steps 3–6 instruct "Open Bank"/"Open Calendar → Plan Today" and doc §3.2/risk-1/risk-3 all require tapping tabs and scrolling under the scrim (the doc's own §3.1 sketch said Stop — the sketch contradicted the doc's behavior text; behavior wins). Now Ignore: full click-through, the card's own buttons unaffected. **Review fix 2 — the clock ran under the tutorial:** nothing held `TimeControlBar`'s `timeFlows` gate, so a fresh save (boots 08:00 @ Normal = 5 min/sec) would burn ~10 game-hours of day-1 needs decay during a ~2-min read — and with fix-1's wall up, Pause was unreachable; doc §7 risk-1 explicitly assumes "a paused, modal surface". Added `GameManager.TutorialOverlayOpen` (set in `Open`/`Close`), a **soft stop** in the Slice-G idiom — new `timeFlows` term + "Paused — tutorial open" bar face, player Speed undisturbed, Skip stays enabled, deliberately NOT in `CanAdvanceDay` (harness-safe plain bool, mirrors the hustle deviation). Checklist A2b un-skipped + 2 new lines covering both fixes. **Independent gate re-run: GrittyEvents 267/267, CoreLoop 22/22, NeedsDecay 102/102, Schema 111/111, MC 345/345 with the MLB guard byte-exact (PA 48384 / H 10969 / ER 5237) — `git diff --stat -- Assets/Simulation` empty. ⚠ One MC run in eight tallied 344/345 (exit 1, no `[FAIL]` line captured); 7 consecutive full-capture re-runs all 345/345 ⇒ pre-existing rare flake in a stochastic check, unrelated to this slice (sim untouched, guard byte-exact) — parked seam upgraded (FAILED-recap-on-tail would have named it).** Build 0/0. **Next: user plays A2 + A2b (incl. the two new fix lines); next build slot = Slice D audio (Opus spec → Fable) or content arc (Opus → Sonnet → Fable) — user picks.**
- **2026-07-11 — Onboarding-2: `TutorialOverlay` BUILT (T-2, Sonnet 5, per Opus 4.8's `docs/design/onboarding_tutorial_overlay.md` §3/§5):** New `Assets/UI/TutorialOverlay.tscn`+`.cs` — a permanent, self-hiding sibling in `Main.tscn` (the `EquipmentShopScreen` scrim+centered-Card idiom), holding the 8-step script verbatim from the doc as data (`TutorialStep[]`, markdown emphasis stripped since `BodyLabel` is a plain `Label`, not BBCode). Spotlighting never reaches into another scene's tree: the overlay emits `TargetRectRequested(int target)` every `_Process` tick while a targeted step is open (doc §7 risk 1 sanction — "a handful of `GetGlobalRect()` calls on a paused modal surface", not the sim loop); `Main` resolves the target against the `BaseballDashboard`/`BurnerPhone` subtrees it already owns (same shared-ancestor bridge as `HustleLaunchRequested`/`ShopOpenRequested`) and answers with `GetGlobalRect()` or an empty `Rect2` (not currently visible — e.g. wrong phone tab) which degrades to a centered, unhighlighted card. Scrim-cutout implemented as four dynamically-resized `ColorRect` notches around the target rect rather than a `_Draw` punch (doc explicitly allows either). New `GameStateKeys.TutorialStep` KV: 1-based step number persisted on every advance (Close writes -1); `Main.TryOpenTutorial` applies the doc §3.3 save-compat guard (absent key + `current_day > 2` ⇒ write -1, never show) and converts the persisted 1-based value to the overlay's 0-based `Open(fromStep)` index at that one boundary. Settings tab gains a third card, **Replay tutorial**, wired via a new `BurnerPhone.TutorialReplayRequested` signal (same bridge pattern) → `TutorialOverlay.Open(0)`. **Self-caught bug during review: an initial off-by-one** (passing the persisted 1-based value straight into the 0-based `Open()`) would have resumed one step ahead of where the player quit — fixed before reporting done (`step - 1` at the Main.cs boundary). **Disclosed scope call:** the doc's optional per-step `DiagramContainer` (§3.1, used by steps 3–5) is populated with a static 5-label decay-order legend rather than a richer diagram — the doc itself says the real teaching is the live spotlighted Needs card, and the three steps' bodies already state the ordering. Zero `Assets/Simulation/` touch, zero schema change (KV row only, matches Slice-G's `TimeOfDayMinutes`/`TimeSpeed` template exactly). `dotnet build`: **0 warnings, 0 errors**. **Gate harnesses NOT re-run this pass** — that and the manual playtest (fresh save walkthrough, quit-mid-tutorial resume, pre-existing mid-career save must NOT show it, Replay button) are next, owned by Fable's review per the doc's T-3 stage description.
- **2026-07-11 — Onboarding-2: needs UI + Free-hours line BUILT (T-1) + `tut_*` tutorial RETIRED (T-3) (both Fable 5, per Opus 4.8's `docs/design/onboarding_tutorial_overlay.md`; retirement of `a8be02b` content user-approved same day):** **T-1** — `ScheduleScreen`'s Free-hours line now three states (0 free = danger *"No free hours — nobody eats today"*, 1..4 = warning *"tight"*, ≥5 = plain explainer; **band harness-sourced, NOT the doc's first-pass 1..3** — `simulate_utility_decay`'s scripted-week fixture bottoms Hunger at 0 even with 5 free hours, so warn through 4, exported as `LowFreeHoursMax`); Needs card gains *"Listed fastest-draining first."* caption, a 20%-line `ColorRect` marker per bar (scene-authored at 0.2, re-anchored in `_Ready` from live `NeedsEngine.CriticalThreshold`), per-row drain/fix tooltips in the .tscn (Hunger's "~16h to critical" is the harness-measured hour-16, per the doc's no-guessed-numbers rule), critical rows turn danger-red + a caption names them (mask-dirty-flagged, plural-aware), card-level acceleration/stress caption (2.5× = `MaxStressModifier`); new shared `UiColors` (danger matches Main.cs's banner red; Slice E absorbs it later). Node paths mapper-verified. **T-3** — `getting_started_events.json` deleted (83→79 events), `RunTutorialArcChecks` + its count pins removed from the gritty harness, `hs_first_day` back on **day 2** (emergent — the tut events just ate the slot; zero event edits), checklist header/A2 day numbers reverted + **Session A2b rewritten** for the overlay slice (T-2 lines marked skip-until-built), `getting_started` contact **kept** as a dormant hint thread (contacts.json + SKILL.md wording updated to say so). **One disclosed deviation from the doc's §6: SKILL.md check #8 KEPT** (the doc's parenthetical said remove it) — it's a general contact-resolution authoring rule whose harness check still runs (now pinning 79); deleting the documentation of a real leak class because its trigger batch retired would be self-harm. Gates: build 0/0, GrittyEvents **267/267**, MC **345/345** MLB guard byte-exact (PA 48384 / H 10969 / ER 5237), CoreLoop 22/22, Schema 111/111, NeedsDecay 102/102 (sourcing run; `Assets/Simulation` untouched — `git diff --stat` confirms UI/harness/content only). **Next: Sonnet 5 builds T-2 → Fable reviews; user plays A2 + A2b.**
- **2026-07-11 — Day-1 phone tutorial Stage-3 review SIGNED OFF (Fable 5):** the Stage-2 build landed as `a8be02b` (auto-committer; the entry below says UNCOMMITTED — stale). Every design-doc §10 item verified: §7.2 alternation (`tut_welcome@2`→`hs_first_day@3`→…→`tut_wrap@8`) + curated `hs_first_day@2` + veteran/College suppression all green in an **independent gate re-run** (GrittyEvents **277/277**, MC **345/345** with the MLB bit-identity guard byte-exact PA 48384 / H 10969 / ER 5237); content-vs-contract clean (3 choices ea., §6.2 flag closure, §6.3 self-contained, §5 copy verbatim modulo the two disclosed reconciliations, both re-verified against the engine: `ContactRegistry.cs` role switch IS closed, choice `text_message` IS singular); `check_event_graph_integrity` #1–#7 pass (avatar scope + `==` gate ⇒ no NULL-team leak; every event self-blocking ⇒ no unpaced loop). **One review fix:** §6.2 told Sonnet to add `getting_started` to "the skill's contact list" — the skill had no such list; added a new check #8 (contact-resolution + shipped contact ids) to `check_event_graph_integrity/SKILL.md`. The `ImagePath`→`LogoPath` harness hunk confirmed a legit compile fix against the real `SchoolCatalog.cs` field. **Next: user plays checklist A2+A2b; then Slice D audio (Opus spec → Fable) or content arc (Opus → Sonnet → Fable) — user picks.**
- **2026-07-11 — Day-1 phone tutorial BUILT (Sonnet 5, UNCOMMITTED), Stage 2 of `docs/design/day1_phone_tutorial.md` (Opus 4.8):** `getting_started_events.json` — 4 avatar-scope events (`tut_welcome`→`tut_daily_loop`→`tut_game_day`→`tut_wrap`, weight 1.0, all `tier==0`), flags-only consequences, `getting_started` contact added to `contacts.json`. Zero engine touch as designed, with two reconciliations against the actual shipped code (both noted in the batch's own header comment): (1) the design doc proposed `role:"system"` for the contact — `ContactJson`'s role parser is a closed switch that throws on an unrecognized string, so per the doc's own contingency this ships on the existing `role:"unknown"` instead; (2) the doc's near-final copy listed TWO delayed texts on one choice each of 3 events, but a choice's `text_message` is a singular field in the engine (one string or one `{body,delay_days}`, never an array) — merged each pair into one message at `delay_days:1`, preserving all the content. Harness: two whole-folder count literals 79→83, new `RunTutorialArcChecks` (content pins + a combined World run proving `tut_welcome@2`→`hs_first_day@3`→alternation through day 8 exactly per the design doc's §7.2 table, deterministic since every beat in both chains is weight 1.0; the skip path forced via `ResolveChoice`; veteran suppression — corrected mid-build: pre-seeding only `tut_done` does NOT suppress `tut_welcome` since each link gates on its own predecessor flag, not the terminal one, so the real invariant is all four flags set; tier suppression at College). GrittyEvents **267→277/277**. Also fixed a pre-existing unrelated build break inherited from `522829f` (school-catalog harness referenced `SchoolDefinition.ImagePath`, which doesn't exist — corrected to the real `LogoPath` field) so `dotnet build` could run at all. Gates: build 0/0, GrittyEvents 277/277, MC 345/345 byte-exact (`git diff --stat -- Assets/Simulation` empty ⇒ inert by construction), CoreLoop 22/22, NeedsDecay 102/102, SchemaValidator 111/111 (no schema touch). Live godot-MCP boot on the real save: **"gritty events 83 (dispatcher polling)"** logged, schema v13/WAL/FK intact, save at day 2 clock-paused, errors empty, clean stop — save left untouched (no day-advance) so the tutorial/onboarding beats stay fresh for the user's own playtest. `docs/hs_manual_playtest_checklist.md` updated: header counts (253→277 GrittyEvents total, 79→83 events, NeedsDecay 94→102), Session A2's day numbers shifted +1 (`hs_first_day`@3, report card ~day 28-29, rival seed ~day 21-22) to reflect `tut_welcome` now winning day 2, new **Session A2b** covering the tutorial thread/skip/heir-reexposure. **Next: Fable 5 Stage-3 review (design doc §10) — ordering/alternation/tier-gate/closure re-check + independent gate re-run + sign-off; then user plays checklist A2+A2b.**
- **2026-07-11 — Slice G BUILT: G-1+G-2+G-3 (Fable 5, UNCOMMITTED):** the full real-time clock per `docs/design/real_time_clock_slice_g.md`. **G-1:** pure `GameClock` in Core (Godot-free, compiled by 3 harnesses; speed ladder Slow/Normal/Fast = 1/5/30 min per real-sec, Faster=120 enum-reserved unsurfaced; one-roll-per-frame back-pressure), `time_of_day_minutes`/`time_speed` Game_State KV keys + boot seed (absent → 08:00 @ Normal), promoted **`GameManager.CanAdvanceDay`** + `InteractiveGameInFlight` (dashboard reports its game task into the shared gate). **Disclosed deviation from the design's §4.1 sketch: `HasPendingHustleSession` is NOT in the hard gate — the hustle screens' shipped contract is that advancing the day IS the no-deal forfeit (NarcoticsHustleScreen abandoned-run note; the can't-afford start panel has no other exit, so a hard gate would soft-lock). The clock treats it as an auto-pause SOFT stop instead; Skip stays enabled.** **G-2:** `TimeControlBar` scene/script replaces the dashboard's Play/Skip pair — Pause/Resume, speed toggles, Skip Day (autopilot ON, verbatim semantics), contextual Play Game (midnight rolls advance with autopilot OFF so game days park pending → auto-pause + button), "TUE APR 14 · 2:47 PM" readout + reason-annotated pause face, settle-wait + `DaySettled`/`PlayGameRequested` signals (at-bat machinery stays in the dashboard); checkpoint persistence via `GameManager.PersistTimeOfDay()` on SaveNow/_ExitTree/pause/speed-change, with a rare-cadence observability print. **G-3:** phone status bar renders the same clock (dirty-flagged day+minute). Gates: build 0/0 (sln ×8), `git diff --stat -- Assets/Simulation` EMPTY → MC inert by construction, CoreLoop 22/22, GrittyEvents 256/256, NeedsDecay 102/102, Schema 111/111 (KV keys ≠ DDL). **Live-verified on the real save: an unattended midnight roll advanced day 1→2 (sqlite-monitored) and the day-2 onboarding event fired off the clock-driven tick (auto-pause face up); pause/speed checkpoint writes print-verified with correct pairs (user clicked the bar mid-run); boot restore resumes the exact persisted minute+speed (booted 628 @ Slow, flowed at exactly 1 min/s); errors empty. Save then RESTORED from backup to pristine day-1 (keys stripped) so the onboarding beat stays unconsumed.** Known seam: the godot MCP's stop is a hard kill, so the `_ExitTree` persist specifically wasn't witnessed — same 4-line path as the click-verified writes; a normal window close exercises it. **Next: user plays (clock/pause/speed/Skip/contextual Play, phone clock lockstep, quit+reload resume); G-4 optional "pause each morning" (Sonnet or Fable, small); next build slot = Slice D audio (Opus spec first) or tutorial thread (Opus outline first) — user picks.**
- **2026-07-11 — Slice G design doc (Opus) — DONE + UNBLOCKED:** `docs/design/real_time_clock_slice_g.md`. Core decision: the intraday clock is a **presentation/driver-layer metronome** — a pure `GameClock` (Core, Godot-free) accumulates real-time→game-minutes and, on midnight, calls the **unchanged** `Clock.AdvanceDay()`; the sim gains NO hour/minute resolution (rejected re-plumbing thousands of entities). Play/Skip reconcile losslessly: Skip Day retained as instant step; continuous clock = paced/interruptible Skip; "Play Game" becomes the contextual at-bat launch. Safety-critical seam = promote the dashboard's private day-advance-blocked predicate to a shared `GameManager.CanAdvanceDay` (auto-pause on any decision gate); one-roll-per-frame back-pressure. Reframe surfaced: 1min=1sec ⇒ 24 real-min/day, so the clock is ambient/tactical, NOT generational traversal (that stays discrete Skip + offseason fast-sim). **User chose Option B (persist time-of-day) — and it's cheaper than first framed: `Game_State` is KV, so `time_of_day_minutes`+`time_speed` are new KV KEYS (like `avatar_practice_credit`), NOT a DDL change — no `user_version` bump, no SchemaValidator change, no migration (absent-key → 08:00 default); checkpoint-only writes via existing `SaveNow`/`_ExitTree`.** Sub-slices G-1 (pure model + KV seed/restore + shared gate) → G-2 (TimeControlBar replaces Play/Skip) → G-3 (phone time-of-day) → G-4 optional "pause each morning". No `Simulation/` touch ⇒ MC inert. **Next: Fable builds G-1.**
- **2026-07-11 — `847d676` verification (Fable 5):** the owed gate record for the parallel-session PlayerScreen/LeagueScreen extraction. `git status` clean at `b68454f` (the doc/skill commit atop it), diff scope confirmed pure `Assets/UI/` + `Main.cs` wiring (no sim/schema touch ⇒ no MC re-run mandated). **Build 0/0** (game + all 6 harness projects), **live boot (godot MCP) on the real save clean:** schema v13, WAL, FK on, day 1 fresh avatar, 48 teams, 819 life-sim NPCs, 79 gritty events dispatcher polling, ~18 s run, **errors empty**. Known-benign: Steam DLL-absent notice (dev environment, pre-existing). **User eyeball same day: Player/League tab layout confirmed correct.** `847d676` is fully closed; UI work may layer on it.
- **2026-07-11 — PlayerScreen/LeagueScreen extraction (`847d676`, parallel session):** dashboard scouting card + stat panels moved into self-contained phone tabs; shipped without a gate run — gate-verified AND user-confirmed live same day (entry above). **CLOSED.**
- **2026-07-11 — Slice B Save+Options (`a7bd114`, Fable 5):** `GameManager.SaveNow()` checkpoint intent + phone Settings tab (Save Now, "Saved ✓ — day N", parked volume slider). All gates green; NeedsDecay 102/102.
- **2026-07-11 — Travel-time checkpoint (`02177f1`, another session's build):** per-trip `TravelTime` + `DaySchedule.TravelHours` + planner forcing; gate-verified then committed pre-B (NeedsDecay 94→102).
- **2026-07-11 — Slice A UI consolidation (`67c9f7b`, Fable 5):** TopBar day+funds, CalendarScreen → phone tab, phone status-bar clock, School slider → mandated note folded into the hours budget.
- **2026-07-11 — HS calendar slice (`b1bc77d`, Fable 5):** sparse 26-game Tue/Fri spring season, Mon–Fri school with breaks, calendar-forced planner blocks, browsable CalendarScreen; MC 345/345, HS band held, MLB guard byte-exact.
- **2026-07-10 — Phone Events/Messages split + per-choice outcomes/delayed texts + category headings + History tab (Sonnet 5 builds, Fable 5 reviews SIGNED OFF):** GrittyEvents 232→256; §4.3 never-gates invariant held; feed capped at 200 cards.
- **2026-07-10 — Act 1 onboarding arc (Opus design → Sonnet build → Fable review) + school-life batch:** `SubjectField.Tier`/`.Gpa`, 11-event onboarding chain, rookie batch `tier>=2` re-gate, +13 school-life events (79 total). **Critical dispatcher fix: avatar hoisted to sweep front — it had been starved of ALL gritty events for 112 days by scope:any NPC saturation of `MaxFiresPerDay`.**
- **2026-07-08…10 — High School arc HS-0…HS-6 + every disclosed seam closed (schema v11→v13):** person layer + backstory/family + phone/marketplace/items economy + GPA drift + dating/pregnancy/child-rearing + NPC autonomy + PersonEffects sim layer + PersonLedger. MC guard byte-exact throughout; the planned HS-6 re-band moved NO band. Exit: user plays `docs/hs_manual_playtest_checklist.md`.
- **2026-07-08 — HS Grade System + freshman start age 16 (`a3fc6cb`-era):** grade derived from age, merit-based graduation, held-back mechanic.
- **Pre-2026-07-08 — Phases 0–12 (see archive):** engine foundation, all harnesses, At-Bat Read-Input model (code-complete, playtest pending), Phase-12 "Saleable" 12a–12d.
